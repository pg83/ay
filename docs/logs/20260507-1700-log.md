# Session log — 2026-05-07 (M2 closure)

## Original request

Session resumed at PR-27 round-1 review (carried over from previous
session that ran out of context). Autonomous mandate from user:
"не прерывай работу, пока она есть. Оставляю тебя за главного,
принимай решения по ходу сам." Mid-session, user injected two
architectural directives that re-opened M2 and required scope
expansion.

## Milestones / PRs worked on this session

5 PRs landed across this session, walking from in-flight PR-27 through
M2 acceptance closure (twice):

- **PR-27** — Parser comparison operators (`==`, `<`, `!=`) + libcxx /
  libcxxrt / libunwind / util walks via DefaultIfEnv expansion +
  cycle-tolerance diagnostic. tools/archiver coverage 1541 → 1696
  nodes. Originally targeted ≥3500 — structural dual-platform gap
  (PR-28+) explained the shortfall.

- **PR-28** — Dual-platform emission via demand-driven host PROGRAM
  closure walk (model (c) — when target enumeration encounters .rl6 /
  asmlib .S, recurse into host ragel6 / yasm and BFS-emit their PEERDIR
  closure under host instance). Closed the 0-host-node cliff:
  1696 → 3321 nodes (1739 target + 1582 host). L0 44.91% → 88.34%.

- **PR-29** — Host-musl CC bundle (composeMuslHostCC 115-arg) +
  per-module CXXFLAGS/CONLYFLAGS/AddIncl threading via ModuleCCInputs +
  clang++/-std=c++20 dispatch + IsGenerated $(BUILD_ROOT) inputPath +
  GLOBAL-modifier filter on CXXFLAGS/CONLYFLAGS. **L3 36.46% → 80.05%**
  (M2 L3 acceptance gate ≥50% cleared).

- **PR-30** — M2 L0 closer: musl/full closure (`defaultProgramPeerdirsFor`
  + ENABLE(MUSL_LITE) per-module routing) + ALLOCATOR_IMPL routing +
  Generator → DepRefs (HasGenerator boolean) + AR-deps cleanup (every
  reference AR has zero AR-on-AR deps) + SRCDIR PROGRAM-only ancestor-
  rebase (composeCCPaths SRCDIR-aware composer with `__/<rel>` infix).
  **L0 88.34% → 98.77%** (M2 L0 acceptance gate ≥95% cleared with
  3.77pp headroom). M2 milestone formally CLOSED against g.json.

- **PR-31** — Triggered mid-session when user clarified canonical
  reference is `sg.json` (NOT g.json); sg.json adds parsed `#include`
  paths to node `inputs`. Comparator switch alone crashed L2 from
  97.56% (vs g.json) to 0.72% (vs sg.json). PR-31 implemented include
  scanner (`scanner.go` + `sysincl.go`, ~1300 LOC) — minimal hand-rolled
  YAML loader for build/sysincl/*.yml + transitive `#include` walk +
  per-path GLOBAL ADDINCL routing + AR/LD/AS member-input aggregation
  + L2 multiset comparator. **L2 0.72% → 79.92%** (M2 L2 acceptance gate
  ≥70% cleared). All 4 M2 acceptance gates now met against sg.json.

## Final M2 acceptance state (against sg.json)

| Gate    | Threshold | Achieved | Headroom |
|---------|-----------|----------|----------|
| L0      | ≥ 95%     | 98.77%   | +3.77pp  |
| L1      | ≥ 80%     | 98.74%   | +18.74pp |
| L2      | ≥ 70%     | 79.92%   | +9.92pp  |
| L3      | ≥ 50%     | 80.35%   | +30.35pp |
| M1      | 2/2 byte-exact | 2/2 ✓ | (preserved) |
| Cycles  | ≤ 14      | 7        | -        |

## Review rounds per PR

- PR-27: round 1 (6 defects, 2 deferred), round 2 (clean). 4 fixes.
- PR-28: round 1 (10 defects, 2 major), round 2 (3 minors), round 3 (clean).
  9 fixes round 2 + 3 fixes round 3.
- PR-29: round 1 (5 defects, 1 major: GLOBAL leaked as literal cmd_arg
  in 116 libcxx CC nodes), round 2 (clean). 5 fixes.
- PR-30: round 1 (5 defects, 0 major), round 2 (1 nit deferred). 4 fixes
  + D05 process boundary (executor edited tasks.md; orchestrator
  overwrote at squash).
- PR-31: round 1 (11 defects, 2 major), round 2 (2 new: D12 deferred
  to PR-32, D13 libcxx/src leak), round 3 (D13 fixed, NO NEW DEFECTS).
  Round-2 fixes (8 code actions) + round-3 fix (D13 per-path GLOBAL).

## Deferred defects (PR-32+ scope)

- **PR-31-D08** — JS emitter doesn't include per-source include closure
  in inputs (~941 entries missing per JS-derived CC; 23 such CCs). Tests
  hard-pinned to current K=8 so future regressions trip.
- **PR-31-D09** — LD emitter composeLDInputs doesn't include peer-archive
  paths in inputs. Tests hard-pinned to K=1020.
- **PR-31-D11** — AR aggregation incomplete (3/38 exact); auto-resolves
  with D12.
- **PR-31-D12 (major)** — Non-musl CC nodes missing 4 musl-arch -I flags
  (`musl/arch/{aarch64,generic,include,extra}`). Root cause: upstream
  `when ($MUSL == "yes") { PEERDIR+=contrib/libs/musl/include }`
  auto-PEERDIRs every TARGET module + ARCH-IF evaluator gap. **Deferred
  to PR-32 musl refactor** because implementing here would bake more
  musl-hardcoding before the dedicated architectural refactor (per
  user directive: "musl выбирается -D флагом в cli, и это всего лишь
  PEERDIR() к каждой библитотеке/программе, в коде yatool не должно
  быть хардкода про это"). Estimated lift if fixed: ~22pp L2 + L3.
- **PR-30-D06** — gen.go:747 comment misnames `genModule` as
  `defaultPeerdirsFor` (1-token nit; deferred).

## User directives received this session

1. **sg.json (not g.json) is canonical reference.** Persisted to memory
   `reference_graph_sg.md`. Triggered M2 re-open + PR-31 scope.
2. **musl is selected by CLI -D flag, NOT hardcoded.** musl knowledge
   scattered across yatool code (~150 hits across 7 files); refactor
   needed. Persisted to memory `musl_not_hardcoded.md`. Scoped to PR-32
   per planner audit.
3. **sysincl/*.yml runtime-parsed** (documented exception to "hand-
   translate build/conf"). Per AskUserQuestion. Persisted to memory
   `sysincl_yaml_runtime_parse.md`.
4. **Bundle peer-propagated GLOBAL ADDINCL into PR-31** (required for
   libcxx headers to resolve; libcxx ~85% of L2 mass). Per
   AskUserQuestion.
5. **musl refactor in PR-32** (after PR-31). Per AskUserQuestion.

## Final ledger state

- M1 [x] (closed earlier session)
- **M2 [x] (closed this session, twice — first against g.json by PR-30,
  then re-opened after sg.json directive, then re-closed against sg.json
  by PR-31)**
- M3 [ ] (Streaming emitter + parallel ya.make parser)
- M4 [ ] (Full host platform; superseded by PR-28's dual-platform pivot)
- M5 [ ] (Compile-flag fidelity hardening)
- M6 [ ] (L3 = 100% on entire reference graph)
- PR-32 [ ] (musl-hardcoding refactor + peer-GLOBAL CFLAGS routing)
- 4 PR-31 deferred defects tracked in defects.md

## Memory persisted

- `reference_graph_sg.md` — canonical reference is sg.json
- `musl_not_hardcoded.md` — musl is CLI-D flag + implicit PEERDIR
- `sysincl_yaml_runtime_parse.md` — runtime-parse exception

## Commits this session

- `9d2c630` PR-27 — parser comparison operators + libcxx/libcxxrt walks
- `a50e46b` PR-28 — dual-platform emission via demand-driven host walk
- `9ed6039` PR-29 prep — plan doc + ledger
- `b341647` PR-29 — host-musl CC bundle + flag threading (L3 lift)
- `64afdb1` PR-30 prep — plan doc + ledger
- `715d877` PR-30 — M2 L0 acceptance closer (M2 first closure)
- `2b94e99` M2 re-opened — sg.json directive
- `cc3c60b` PR-31 prep — include scanner plan
- `1a8b02a` PR-31 — include scanner + sg.json + GLOBAL ADDINCL (M2 final closure)

## Open questions for next session

None. M2 is closed; PR-32 (musl refactor) is well-scoped in defects.md
PR-31-D12 + memory `musl_not_hardcoded.md` + PR-31 plan doc Note B.
Ready to start PR-32 in a fresh session, or continue with M2 polish
PRs (D08/D09/D11/D12) if user prefers L2/L3 lift before architectural
refactor.

# Yatool — Recreate ymake build-graph generator — Task Ledger

Authoritative ledger of planned and completed work. Detailed plan: `./docs/drafts/20260507-0134-recreate-ymake-plan.md`.

Status: `[ ]` planned · `[~]` in progress · `[x]` done · `[!]` blocked

---

## Milestones (high-level)

- [x] **M1** — Single-leaf vertical slice: parse one zero-PEERDIR LIBRARY's ya.make, emit one CC + one AR node, comparator wired and reporting all 4 levels. Acceptance: L3 = 100% on the chosen 2-node subgraph. **CLOSED 2026-05-07 with `./yatool compare --level=3 our.json /yatool_orig/g.json` → 2 matched / 2 pairs / 0 unpaired-want / 3728 unpaired-got at all 4 levels.**
- [~] **M2** — `tools/archiver` static slice (CC + AR + LD, recursive PEERDIR, host platform). Acceptance gates met against sg.json by PR-31 (2026-05-07): L0=98.77%, L1=98.74%, L2=79.92%, L3=80.35%. PR-32/33 polish: L3 → 95.60%. **Re-opened 2026-05-08** when user added HARD perf gate (`./yatool gen --target tools/archiver` ≤5s; currently 28s). PR-34 must close perf gap before M2 can finally close.
- [ ] **M3** — Streaming emitter + parallel ya.make parser. Acceptance: peak RSS < 200 MB, wall ≤ 1.5× sequential.
- [ ] **M4** — Full host platform (tool transitive closure + foreign_deps merge). Acceptance: L0 = 100%, L1 ≥ 95%.
- [ ] **M5** — Compile-flag fidelity (hand-translate gnu_compiler.conf + settings.conf). Acceptance: L3 ≥ 95%.
- [ ] **M6** — Final L3 = 100% on the entire reference graph.

---

## Milestone 1 — PR breakdown

Detail in `./docs/drafts/20260507-0134-recreate-ymake-plan.md`. One line per PR here; sub-tasks stay in the plan doc.

- [x] **PR-01** — Bootstrap: `go.mod`, `main.go`, subcommand router (`gen`/`compare`/`inspect`/`help`). Stdlib-only.
- [x] **PR-02** — `node.go`, `emitter.go`, `uid.go`: Node type, Emitter interface, BufferedEmitter, Merkle UID finalizer.
- [x] **PR-03** — `gjson.go`: streaming reader for the reference g.json.
- [x] **PR-04** — `compare.go` + `compare_topology.go`: comparator L0 (topology fingerprint).
- [x] **PR-05** — `compare_props.go`: comparator L1 (kv.p, module_*, outputs) + L2 (inputs, tags, requirements).
- [x] **PR-06** — `compare_cmd.go`: comparator L3 (cmd_args + env byte-exact).
- [x] **PR-07** — `yamake.go`: minimal ya.make parser (LIBRARY/PROGRAM/PEERDIR/SRCS/SET/END).
- [x] **PR-08** — `cc.go`, `flags.go`, `toolchain.go`: minimal CC rule with hardcoded flag bundles.
- [x] **PR-09** — `ar.go`: minimal AR rule.
- [x] **PR-10** — `gen.go`: gen subcommand wires PR-07/08/09; emits 2-node subgraph; integrated comparator pass shows L3 = 100% on the slice.
- [x] **PR-11** — Retrofit existing code to STYLE.md (`throw.go` for non-discriminated errors; formatting per "Blank lines around control blocks" / "Blank lines before return" / "Logical grouping"). Touches `main.go`, `node.go`, `emitter.go`, `uid.go`, `yamake.go` and their tests where blocking. Acceptance: tests still pass; reviewer audit confirms STYLE.md compliance. **Must land BEFORE PR-03** so the comparator/parser PRs are authored over compliant base.

---

## Milestone 2 — PR breakdown

Detail in `./docs/drafts/20260507-0549-m2-plan.md`. One line per PR here.

- [x] **PR-12** — Generalize `Gen()` for recursive PEERDIR walk; accept PROGRAM; refactor EmitCC signature `(NodeRef) → (NodeRef, string)`. **Wave 1 — blocks all Wave 2.**
- [x] **PR-13** — **SUPERSEDED by PR-23 cherry-pick.** Parser extension + macros.go landed verbatim in PR-23. Worktree retained for git history.
- [x] **PR-14** — **SUPERSEDED by PR-23 cherry-pick.** Musl flag bundles landed in PR-23 `flags.go`; ModuleFlavor type discarded (subsumed by `ModuleInstance.Flags`).
- [x] **PR-15** — **SUPERSEDED by PR-23 cherry-pick.** `ar.go` body landed with `EmitAR(instance, ...)` signature update.
- [x] **PR-16** — `as.go` (AS rule for `.S` files). [Wave 2, parallel] **REWORKED in-place by PR-23** (signature → `EmitAS(instance, srcRel, yasmLD NodeRef, emit)` per D33; `ForeignDepRefs["tool"]` wiring per D31).
- [x] **PR-17** — **SUPERSEDED by PR-23.** `cp.go`/`js.go` cherry-picked with signature update. `r6.go` rewritten in PR-23 with `ragel6LD NodeRef` parameter per D31 (no stub UID). `emitter.go` Finalize-relax never landed (PR-17 wasn't merged before the pivot).
- [x] **PR-18** — `Emit`/`Result` panic-on-finalized guards. [Wave 2, parallel] **Survives unchanged** (orthogonal to D30 addressing).
- [x] **PR-28** — Dual-platform emission (host PROGRAM closure walk). Demand-driven (model (c) per `docs/drafts/20260507-1131-pr28-dual-platform.md`): when target walk encounters `.rl6`/`.S(asmlib)`, recurse into host ragel6/yasm and BFS-emit their PEERDIR closure under host instance. Closed 0-host-node cliff: 1696 → 3321 nodes (1739 target + 1582 host). L0 44.91% → 88.34%, L1 → 88.66%, L2 → 86.89%, L3 → 36.46%. Plus PROGRAM(name) honoring + SRCDIR threading (also fixes JOIN_SRCS via D11) + ALLOCATOR macro resolution + R6 deps inversion fix.
- [x] **PR-29** — L3 lift via host-musl CC bundle + flag threading. composeMuslHostCC (115-arg bundle), per-module CXXFLAGS/CONLYFLAGS/ADDINCL threading via ModuleCCInputs struct, clang++ + -std=c++20 dispatch, NO_COMPILER_WARNINGS bundle selector, IsGenerated $(BUILD_ROOT) inputPath, GLOBAL-modifier filter for CXXFLAGS/CONLYFLAGS. **L3 36.46% → 80.05% (+43.6pp; M2 L3 acceptance gate ≥50% cleared).** L2 86.89% → 87.53%; L0/L1 preserved.
- [x] **PR-30** — L0 closer: musl/full closure + ALLOCATOR_IMPL routing + Generator→DepRefs + AR-deps cleanup + SRCDIR PROGRAM-only rebase. **L0 88.34% → 98.77% (clears M2 acceptance gate ≥95% with 3.77pp headroom). M2 ALL FOUR GATES MET.** L1 88.66% → 98.74%; L2 87.53% → 97.56%; L3 80.05% → 80.35%.
- [x] **PR-31** — Include scanner + sg.json reference switch + per-path GLOBAL ADDINCL routing. **L2 0.72% → 79.92% (M2 acceptance gate ≥70% MET).** New: `scanner.go` + `sysincl.go` (~1300 LOC); modified all rule emitters for member-input aggregation. Plan: `docs/drafts/20260507-1544-pr31-include-scanner.md`.
- [x] **PR-32** — musl refactor (flag-driven, no path-prefix dispatch) + auto-PEERDIR `contrib/libs/musl/include` (PR-31-D12 closure) + peer-propagated GLOBAL CFLAGS/CXXFLAGS/CONLYFLAGS routing (PR-29-D04 carry-over) + `--define KEY=VALUE` CLI. **L2 79.92% → 83.27% (+3.35pp); L3 80.35% → 80.51% (+0.16pp).** L0/L1 preserved. M1 byte-exact preserved. Path-prefix `HasPrefix("contrib/libs/musl")` dispatch eliminated from production code (1 documented backward-compat shim remaining). Util-subtree libcxx/libcxxrt peering (the L2 ceiling at 83.27%) deferred to PR-33.
- [x] **PR-33** — runtimeAncestorPaths literal-only + own GLOBAL ADDINCL/CFLAGS/CXXFLAGS routing + non-GLOBAL CFLAGS threading + cxx-standard-warnings bundle + util PEERDIR ordering hoist (C01) + yasm `-D_musl_=1` PROGRAM injection (C02). **L3 80.51% → 95.60% (+15.09pp);** L2 83.27% → 83.94% (+0.67pp; sysincl ceiling deferred to PR-35); L0/L1 preserved; M1 byte-exact preserved.
- [~] **PR-34** — **PERF EMERGENCY: `./yatool gen --target tools/archiver` must complete in ≤5s (currently 28s, 5.6× over).** User-imposed hard acceptance criterion 2026-05-08. Profiler dispatched; PR scope TBD post-profiling.
- [ ] **PR-35** — (was PR-34; renumbered.) sysincl per-record source-vs-includer routing (PR-31-D08/D09 + PR-33-C03 uchar.h fan-out). L2 lift 83.94% → ~90.7%. Plus deferred PR-33 minor/nit cleanups (B01-B08, A2_03-A2_08, C2_02-C2_07).
- [ ] **PR-34** — JS/LD scope expansion (PR-31-D08/D09): JS-derived CC nodes' include closures + LD peer-archive paths in inputs.
- [ ] **PR-19** — `ld.go` 4-cmd LD rule + Gen wiring. Renumbered as **PR-24**. After PR-23.
- [ ] **PR-20** — `gen.go` walker + macro evaluator wiring + host-tool recursion hooks. Renumbered as **PR-25**. After PR-24. **Wave-3 keystone.**
- [ ] **PR-32** — M2 acceptance gate. **No `--filter-platform`** (D40). Acceptance: L0 ≥ 95%, L1 ≥ 80%, L2 ≥ 70%, L3 ≥ 50% on FULL graph. Runs after PR-28..PR-31 close coverage and tuning gaps.
- [x] **PR-23 (PR-PIVOT)** — **Architectural pivot.** Introduces `ModuleInstance`/`FlagSet`/`Language`/`PlatformID`. Cherry-picks salvageable Wave-2 code. Reverts PR-17 emitter.go. Adds `Emitter.OnReady`. Acceptance: M1 4-node build/cow/on (target+host) byte-exact at L0/L1/L2/L3. **Plan: `./docs/drafts/20260507-0717-m2-architecture-revision.md`.**
- [x] **PR-24** — `ld.go` 4-cmd LD rule + Gen wiring for PROGRAM-with-LD. After PR-23.
- [x] **PR-25** — Walker integrates macro evaluator + per-instance flag derivation + host-tool recursion (ragel6/yasm). Wave-3 keystone.
- [x] **PR-26** — Hardcode implicit DEFAULT_PEERDIRs (musl, builtins, malloc/api). Walker expanded 50→1541 nodes (~30× jump). L0=40.91%, L1=41.31%, L2=41.26%, L3=35.60%. libcxx/libcxxrt/libunwind/util deferred to PR-27 (need parser extension for `==`/`<` operators).
- [x] **PR-27** — Extend yamake.go parser Expr ADT with `IF (X == "Y")` and `IF (X < N)` operators. Bind CXX_RT/ANDROID_API/etc in DefaultIfEnv. Unblocks libcxx/libcxxrt/libunwind/util walks. Brings tools/archiver coverage 1541 → 1696 nodes; ≥3500 target unmet — structural dual-platform gap (PR-28+).

---

## Cross-cutting architectural notes (locked)

- [x] **D1** — Module path: `module yatool` in `go.mod`.
- [x] **D2** — Layout: `go.mod` + ALL `.go` files at `/home/pg/monorepo/yatool/*.go` (flat, `package main`). Only allowed new dirs under repo root: `docs/drafts/`, `docs/logs/`. No `internal/`, no `cmd/`. Lands in PR-01.
- [x] **D3** — Single binary, stdlib `flag.NewFlagSet` subcommand router. Subcommands: `gen`, `compare`, `inspect`, `help`. Lands in PR-01.
- [x] **D4** — JSON: `encoding/json` with `json.Decoder` for streaming reads. No goccy/simdjson until profiling proves need.
- [x] **D5** — `Node` mirrors on-disk JSON exactly; `omitempty` only where g.json itself omits (`host_platform`, `foreign_deps`). UIDs are placeholders until finalizer pass. Lands in PR-02.
- [x] **D6** — `Emitter` interface with `NodeRef` indirection so M1's BufferedEmitter is swappable for M3's StreamingEmitter without rewriting rules. Lands in PR-02.
- [x] **D7** — UID = `base64url(sha1(canonical-node-bytes))[:22]` Merkle-style. Comparator L0 uses topology fingerprints, never raw UID strings. Lands in PR-02.
- [x] **D8** — Two-pass engine: `Generate(cfg PlatformConfig, target string, emit Emitter)` invoked twice (Target/Host) + merge step. M1 stubs host pass.
- [x] **D9** — One Go file per op-type (`cc.go`, `ar.go`, `ld.go`, …). Flag bundles in `flags.go` as named `[]string`s composed at call time. Toolchain constants in `toolchain.go`. Parser in `yamake.go`.
- [x] **D10** — Comparator co-resident: `compare.go` + `compare_topology.go` + `compare_props.go` + `compare_cmd.go`.
- [x] **D11** — Reference data: `/home/pg/monorepo/yatool_orig/g.json` read in place. No fixture copy.
- [x] **D12** — `build/scripts/*.py` referenced by `$(SOURCE_ROOT)/build/scripts/<name>.py` in cmd_args, never re-implemented.
- [x] **D13** — Stdlib-only through M1. Vendor decision deferred to M3 (open question Q2).
- [x] **D14** — Determinism: NO `range` over `map[...]X` in any code path that emits node fields. `sort.Strings` before serialize. `json.Encoder.SetEscapeHTML(false)`.
- [x] **D15** — `inputs`/`conf` in our generator output may be empty; comparator only reads `graph` + `result`. Revocable.
- [x] **D16** — JSON serializer in PR-04 (and any future graph-writing code) MUST use `json.Encoder` with `SetEscapeHTML(false)` to produce output bytes that match `canonicalNodeBytes` from PR-02. Otherwise default `json.Marshal` HTML-escapes (`<` → `<` etc.), producing on-disk bytes the Merkle hash never saw, breaking comparator. Pinning test landed in PR-02 (`TestCanonicalNodeBytes_VsDefaultJSONMarshal`). Origin: PR-02-D08.
- [x] **D19 — M2 recursive PEERDIR walk.** Depth-first, post-order, cycle-detected, memoized by targetDir. Visit order = declaration order inside `PEERDIR(...)` (preserve, not sort) — produces LD link-order R14. M2 single-threaded; M3 parallelizes.
- [x] **D20 — M2 stub-host strategy.** Host-tool foreign_deps populated with hardcoded reference-graph UIDs from `host_stubs.go`. **Verified by PR-17 executor 2026-05-07** (planner mislabel corrected): the R6 node `util/_/datetime/parser.rl6.cpp` actually uses `foreign_deps.tool = ["XO1d8CLk3qDKv0XQTlDKmQ"]` (ragel6 LD UID — dangling external from PR-15's perspective; resolved internally to ragel6 host LD by reference graph). `OsvsEu9xnOqLNXi3INZ9CQ` is the yasm binary's UID (no current consumer in archiver closure). M4 replaces with real host pass.
- [x] **D21 — Multi-source CC.** One `EmitCC` per source, declaration order (not sorted). PR-10-D03 refactor: `EmitCC` returns `(NodeRef, string)` — lands in PR-12.
- [x] **D22 — LD rule shape.** ONE Node with FOUR Cmds (vcs_info → clang compile-vcs → link_exe.py → fs_tools.py link_or_copy_to_dir). Single `EmitLD(...)` in `ld.go`. Lands PR-19.
- [x] **D23 — Accept PROGRAM in addition to LIBRARY.** PR-12 gates this; PR-19 emits real LD for PROGRAM.
- [x] **D24 — `--target` for `gen` accepts arbitrary module dir.** No CLI shape change.
- [x] **D25 — Recursive parser parallelism.** Single-threaded for M2. M3 introduces `golang.org/x/sync/errgroup`.
- [x] **D26 — JSON-diff library for L3 humans.** Defer until M5/M6.
- [x] **D27 — Macro evaluator.** Tiny ad-hoc evaluator. Supports `IF(IDENT)`, `AND`, `OR`, `NOT`, parens. Bound vars hardcoded in `macros.go`. Hard error on unknown identifier.
- [x] **D28 — Parser AST extensions for M2** — `IfStmt`, `IncludeStmt`, `JoinSrcsStmt`, `AddInclStmt`, `CFlagsStmt`, `LDFlagsStmt`, `SrcDirStmt`, `GlobalSrcsStmt`. NO_*/LICENSE/etc stay as `UnknownStmt`.
- [x] **D29 — Output path mangling for non-`.cpp` sources.** `.S` → `$(BUILD_ROOT)/<moduleDir>/_/<basename>.o`. `.rl6` → intermediate `$(BUILD_ROOT)/<moduleDir>/_/<src>.cpp`.

### Architecture revision (2026-05-07) — D30–D40 supersede D8/D19/D20/D21/Q5

User feedback: module = tuple `(path, language, target, flag-set)`, not path alone. Cross-platform via re-instantiation, not post-merge. Early execution requires real refs throughout. Plan: `./docs/drafts/20260507-0717-m2-architecture-revision.md`. Lands in PR-23 (PR-PIVOT).

- [x] **D30** — `ModuleInstance{Path, Language, Target PlatformID, Flags FlagSet}` is the address. Comparable by value; memo key. Supersedes D8/D20/D21.
- [x] **D31** — Cross-platform recursion replaces post-merge join. Walker recurses into `instance.WithHost(cfg)` for tool deps; gets real `NodeRef`. No stub UIDs. Supersedes D8.
- [x] **D32** — One Emitter, one Graph, no merge step. Both target and host nodes accumulate in single `BufferedEmitter`. `PlatformConfig{Target, Host PlatformSpec}`. Supersedes D8.
- [x] **D33** — Rule signatures take `instance ModuleInstance` (NOT `cfg + moduleDir`). Output paths use `instance.Flags.PIC` for `.o` ↔ `.pic.o`, `.a` ↔ `.pic.a`. Supersedes D21.
- [x] **D34** — `genCtx.memo: map[ModuleInstance]*moduleEmitResult`. Same path different (target/host/lang/flags) memoize separately.
- [x] **D35** — Language-polymorphic macros via dispatch table (per-language `LanguageEmitter`). PROTO/FBS/etc reserved; M5+ implementation. M2 ships only LangCPP.
- [x] **D36** — Walker is post-order, recursion-driven, single emitter. PEERDIR declaration order preserved (R14). Cycle detection covers within-platform AND cross-platform. Supersedes D19.
- [x] **D37** — `Emitter.OnReady(NodeRef) <-chan struct{}` lands in PR-23 (interface; BufferedEmitter no-op impl). M3 StreamingEmitter implements per-node. Enables early execution.
- [x] **D38** — Comparator continues to receive static `*Graph` from Finalize. Streaming impl assembles same struct.
- [x] **D39** — `Flags.PIC` is canonical host/target axis for M2. Drives output mangling, `-fPIC` inclusion, flag-bundle selection.
- [x] **D40** — `--filter-platform` descoped (revises Q5). Direct recursion emits both platforms; comparator pairs by `(outputs[0], platform)` already.

Q-resolutions for PR-23:
- **Q6** — Type names locked: `ModuleInstance`, `Language`, `PlatformID`, `FlagSet`.
- **Q7** — PR-PIVOT lands as ONE squash commit per CLAUDE.md "One PR = one commit".
- **Q8** — M3 StreamingEmitter impl deferred; `OnReady` interface lands in PR-23.
- **Q9** — Confirmed by probe: `foreign_deps.tool` semantics permit recursion model. Host yasm closure already part of full graph.
- **Q10** — PR-23 acceptance: target byte-exact + host structural (4 nodes total). Full host byte-exact = M4.
- [x] **D17** — All error handling goes through `throw.go` per `STYLE.md` "Error handling". Pass-through `if err != nil { return err }` is forbidden — use `Throw`/`Throw2`/`Throw3`/`ThrowFmt`. Functions return `error` only when (a) interface obligation (e.g. `flag.Value.Set`, `json.Unmarshaler`), (b) error is a domain signal the caller discriminates via `errors.As`/`errors.Is`, or (c) external boundary (CLI/RPC). Top-level `main` and each goroutine entry MUST wrap body in `Try(...).Catch(...)`. Retrofit of existing code lands in PR-11. Reviewers MUST flag any pass-through that doesn't fit the three exception categories.
- [x] **D18** — Code follows `STYLE.md` "Formatting": blank lines around control blocks (`if`/`for`/`switch`/`select`/`go`/`defer`) except first/last in a `{}`; blank line before `return` except first stmt after `{`; consecutive one-liners that form one logical operation stay together (no blanks), separate ops get a blank between. Reviewers MUST run a quick visual check; nit-tier defects but accumulate into a noisy diff if missed. Retrofit of existing code lands in PR-11.
- [x] **Q1** — M1 leaf module: **`build/cow/on`**. Single C source (`lib.c`), zero PEERDIR, ya.make = `LIBRARY()/NO_UTIL/NO_LIBC/NO_RUNTIME/SRCS(lib.c)/END()`. Reference subgraph in g.json: 1 CC node (`lib.c.o`, 101 cmd_args, `clang` not `clang++`) + 1 AR node (`libbuild-cow-on.a`, 11 cmd_args, standard `link_lib.py` invocation). No host-tool deps, no foreign_deps. Inspection report: see Q1 subagent return on 2026-05-07. PR-08 hardcodes the 101-arg CC bundle; PR-09 hardcodes the 11-arg AR pattern; PR-10 wires parser+rules to produce the 2-node subgraph.
- [x] **Q2** — Resolved 2026-05-07: **any third-party libs allowed** from M2 onward (per user decision). Vendor under `vendor/` if added; stdlib still preferred where it suffices but not mandatory.
- [x] **Q3** — M2 acceptance gate relaxed: **L1 ≥ 80%** (was 85%) / L3 ≥ 50%. Hitting 85% requires hand-translating cxxsupp/builtins + abseil flag bundles (M5 territory bleeding into M2). Musl-only bundle in M2 keeps L1 in the 80-85% band.
- [x] **Q4** — Stub-host UIDs hardcoded from reference graph (`OsvsEu9xnOqLNXi3INZ9CQ` for ragel6, `XO1d8CLk3qDKv0XQTlDKmQ` for dangling external). Preserves L0/L1/L2 fingerprint pairing. M4 replaces with real host pass.
- [x] **Q5** — Add `--filter-platform <name>` to `compare` subcommand (lands in PR-22). Compares against target-only subset; otherwise our 1,926-node emit looks like ~52% of the full 3,730-node graph regardless of correctness.

---

## Completed

- **PR-01** (2026-05-07) — Bootstrap of the Go module and subcommand router. Files added: `go.mod` (`module yatool`, `go 1.25`, no `require`/`toolchain`); `main.go` (single file, `package main`, hand-written switch dispatcher on `os.Args[1]` per D3, four subcommands `gen`/`compare`/`inspect`/`help`, three stub bodies that print `<name>: not implemented yet` and return 1, top-level help on stdout exit 0, no-args/unknown-subcommand exit 2 with usage on stderr).
  Verification: `go build ./...` 0; `go vet ./...` clean; `gofmt -l *.go` empty; `./yatool help`→0; `./yatool`→2; `./yatool gen`→1; `./yatool gen -h`→1 (post-fix); `./yatool gen --help`→1; `./yatool gen --foobar`→1; `./yatool gen foo bar`→1; `./yatool compare -h`→1; `./yatool inspect --foobar`→1; `./yatool wat`→2; `./yatool --help`→0; `./yatool -h`→0.
  Two review rounds. Round 1 (4 defects):
  - **D01 (major)** + **D02 (minor)** + **D03 (nit)** — `flag.NewFlagSet(name, flag.ExitOnError).Parse(args)` in stubs short-circuited `-h`/`--help`/unknown-flag paths to flag's auto-usage and exited 0/2 BEFORE the stub message printed. **Fixed** by removing `fs.Parse(args)` from all three stubs and switching the parameter to `_ []string` (`main.go:50-69`).
  - **D04 (nit)** — orchestrator's `tasks.md` `[ ]` → `[~]` flip was uncommitted at review time; reviewer flagged correctly per brief. **Resolved** by staging `tasks.md` with the PR commit (this entry).
  Round 2 (1 nit):
  - **D05 (nit)** — surviving `_ = flag.NewFlagSet(...)` in stubs is dead ceremony; `"flag"` import is dead-loaded. **Deferred to PR-10** which will rewrite stubs to register real flags and naturally remove the ceremony.
  Surprises / constraints future PRs must respect:
  - **D3 letter vs. spirit:** "each subcommand uses `flag.NewFlagSet`" was preserved in PR-01 only as `_ = flag.NewFlagSet(...)`. PR-10 (`gen` driver) MUST replace these three lines with real flag registration + Parse, and either keep `"flag"` as a load-bearing import or drop it. Failure to do so leaves dead code in `main.go`.
  - **Flag-mode choice for stubs vs. real subcommands:** `flag.ExitOnError` shipped to satisfy spec letter, but is dangerous for stubs because it short-circuits `-h` to `os.Exit(0)`. PR-10 should use `flag.ContinueOnError` (or override `fs.Usage` + `SetOutput`) so the subcommand body retains control of exit semantics. This pattern will recur for every subcommand that adds flags — codify it in the PR-10 brief.
  - **Exit-code domain split:** `1` = recognised-but-not-implemented; `2` = yatool's argument-level errors (no subcommand, unknown subcommand). Future subcommands' real argument errors should also exit `2`; their domain-specific failures should exit `1` or `>2` (TBD). Keep the split visible.
  - **Review-brief hygiene:** Future review briefs MUST enumerate expected ledger churn (`tasks.md` `[~]` flip, `defects.md` round entries) so reviewers don't flag legitimate orchestrator bookkeeping as undeclared scope drift (D04).

- **PR-02** (2026-05-07) — Foundation types: `Node` (mirrors on-disk JSON exactly, snake_case tags, `omitempty` on `host_platform`/`foreign_deps` only, internal `*Refs` fields with `json:"-"`), `Cmd` (cmd_args + env), `NodeRef` (id-based placeholder), `Emitter` interface with `Emit`/`Result`, `BufferedEmitter` impl, `Graph` wrapper, `Finalize` (topological Kahn's sort with stable tie-break, Merkle-style UID computation, sorted+deduped Deps/ForeignDeps, dedupe of identical-UID nodes and duplicate Result calls, second-call rejection via `finalized` flag, pre-populated Deps/ForeignDeps rejection). `computeUID = base64url(sha1(canonicalBytes))[:22]`. `canonicalNodeBytes` zeroes UID/SelfUID/StatsUID and marshals with `SetEscapeHTML(false)`. Files: `node.go`, `emitter.go`, `uid.go` + 3 test files. Verification: `go build/vet/gofmt` clean; `go test -count=1` 26/26 PASS.
  Two review rounds. Round 1 (8 defects, 0 major):
  - **D01 (minor)** — test pinned wrong `SelfUID == UID` invariant (real graph has 101 nodes where they differ). **Fixed** by switching to `t.Logf` placeholder + `// TODO(future-PR)` at SelfUID assignment.
  - **D02 (minor)** — pre-set `Node.Deps`/`Node.ForeignDeps` silently corrupted Merkle hash. **Fixed** by rejecting at Finalize entry with concrete error; tests `TestFinalize_RejectsPreSet{Deps,ForeignDeps}`.
  - **D03 (minor)** — no UID-dedupe in `Graph.Graph` (two identical emits → two entries with same UID). **Fixed** by `seen map[string]struct{}` skip in final loop; test `TestFinalize_DedupesIdenticalEmits`.
  - **D04 (nit)** — comment claimed re-Finalize would "observe missing input" — false. **Fixed** by adding `finalized bool` to BufferedEmitter, returning error on second Finalize; test `TestFinalize_SecondCallErrors`. Comment updated to match.
  - **D05 (nit)** — empty-value `ForeignDepRefs` key serialized as `foreign_deps:{key:[]}`. **Fixed** by skipping empty resolved keys; nil ForeignDeps drops via omitempty; test `TestFinalize_DropsEmptyForeignDepsKey`.
  - **D06 (nit)** — duplicate `Result(ref)` calls produced duplicate UIDs. **Fixed** by dedupe in Finalize preserving first-seen order; test `TestFinalize_DedupesDuplicateResultCalls`.
  - **D07 (nit, deferred)** — O(n²) topo tie-break linear scan. Deferred to a profiling-driven PR (replace queue with `container/heap` if it shows in profile).
  - **D08 (minor)** — PR-04 must use `SetEscapeHTML(false)` to match `canonicalNodeBytes` output. **Fixed** by adding cross-cutting note D16 in tasks.md + pinning test `TestCanonicalNodeBytes_VsDefaultJSONMarshal` that asserts default `json.Marshal` and `canonicalNodeBytes` produce DIFFERENT bytes for HTML special chars (locks the contract in code).
  Round 2 (2 nits, both deferred):
  - **D09 (nit, deferred)** — `Emit`/`Result` don't check `finalized`; silent post-Finalize mutation. Defer-trigger: when Emitter interface is revisited (M3 streaming-emitter is the natural pivot).
  - **D10 (nit, deferred)** — partial-empty `ForeignDepRefs` (one key empty, one not) untested. Defer-trigger: when comparator catches a real `foreign_deps` divergence in M2.
  Surprises / constraints future PRs must respect:
  - **D16 cross-cutting (PR-04 escape contract):** any code that serializes a `*Node` to JSON for on-disk emission MUST use `json.Encoder.SetEscapeHTML(false)`. Default `json.Marshal` HTML-escapes `<`/`>`/`&` → bytes the Merkle hash never saw → comparator divergence with no apparent root cause. Pinning test in PR-02 (`TestCanonicalNodeBytes_VsDefaultJSONMarshal`) will fail loudly if any future serializer drifts.
  - **JSON key order from struct field declaration order:** Go's `encoding/json` orders fields by struct declaration order. The Node struct declares fields ALPHABETICALLY (verified by inspection of g.json — that's the on-disk order). Any new field added to Node must be inserted in alphabetical position; otherwise serialized output diverges from g.json key order, breaking byte-exact comparator at L3.
  - **`Requirements` is `map[string]interface{}`, not `map[string]string`** — g.json mixes int/str values (`{cpu: 1, network: "restricted", ram: 32}`). Any rule emitting Requirements must use the interface type. Tests catch wrong type at compile time.
  - **`stats_uid` is empty string placeholder.** Real g.json has 32-char MD5-hex values. When real semantics land, `D01`'s placeholder test guard converts back to invariant assertion.
  - **Pre-populated `Node.Deps`/`Node.ForeignDeps` is a hard error, not a warning.** Rules MUST use `DepRefs`/`ForeignDepRefs` only. Diagnostic: `finalize: node N has pre-populated Deps; rules must use DepRefs only`.
  - **`BufferedEmitter` is single-use.** Cannot be re-finalized. M3's streaming emitter will need its own lifecycle semantics.

- **PR-07** (2026-05-07) — Hand-rolled `ya.make` parser. Lexer (1-based line/col tracking, `\r`/`\n`/`\r\n` all bump line by 1, `#`-comments only at whitespace boundary, raw strings with no escape processing) + recursive-descent parser. Recognized macros: `PROGRAM`/`LIBRARY` → `ModuleStmt`; `PEERDIR` → `PeerdirStmt`; `SRCS` → `SrcsStmt`; `SET(name value)` (arity 2 enforced) → `SetStmt`; `END` → `EndStmt`. Anything else (lower/mixed-case macros included, e.g. `lowercase_macro()`) → `UnknownStmt` (permissive parsing). API: `Parse(name, src)` and `ParseFile(path)`. Errors via `*ParseError{File, Line, Col, Message}`. Files: `yamake.go`, `yamake_test.go`. Verification: `go build/vet/gofmt` clean; 16 test functions PASS (incl. CRLF/CR/LF line-ending subtests, mixed-case macro routing, `isWordByte` boundary probe).
  Two review rounds. Round 1 (10 defects, 2 major):
  - **D01 (major)** — unit tests hard-coded `/home/pg/monorepo/yatool_orig/...` paths; D11 says unit tests use synthetic inputs. **Fixed** by inlining ya.make snippets as Go raw strings; one optional disk-read smoke test (`TestParseArchiverYaMakeOnDisk`) gated by `t.Skip` when file missing.
  - **D02 (major)** — `\r` (lone CR / Mac-classic line endings) not bumping line counter; CRLF column off. **Fixed** in `advance` — `\r` and `\n` both bump line by 1 reset col, `\r\n` swallows the `\n`. Tests cover all three line endings.
  - **D03 (minor)** — strings span newlines silently (missing close-quote swallows downstream). **Fixed** by rejecting `\n`/`\r` in string body with `unterminated string` pinned at opening quote.
  - **D04 (minor)** — mixed-case macro names (`Foo()`, `lowercase()`) hard-erred instead of routing to `UnknownStmt`. **Fixed** via `isIdentShapedName` helper that accepts ident-shaped tokens followed by `(` as macro names.
  - **D05 (minor)** — `isWordByte` too permissive (`` ` ; | & ^ < > [ ] ' `` accepted). **Fixed** by trimming to `_ - . / + : = * ? $ % ~ , ! { } #` (kept `{}` for `${VAR}`, kept `#` because `skipTrivia` gates).
  - **D06 (minor)** — `# mid-word` started a comment. **Fixed** via `commentBoundary` helper consulting `prevByte` (whitespace/SOF/`(` only).
  - **D07 (nit, deferred)** — `nil` vs `[]string{}` for empty arg lists undocumented. AST internal; revisit when JSON-serializing AST.
  - **D08 (minor)** — backslash-in-string semantics ambiguous, no test. **Fixed** by KEEPING "no escape processing" + pinning test `TestStringHasNoEscapeProcessing` + code comment.
  - **D09 (nit, deferred)** — multi-line macro EOF position robustness untested (correct by inspection).
  - **D10 (nit, deferred)** — `foo"bar"baz` lexes as 3 tokens (greedy lexing, surprising but no real-world hit).
  Round 2: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **`# mid-word` is now part of the word.** A bare `a/b#x` parses as one word ending at the `#` is no longer the case — it's all word bytes. Comments only fire at whitespace boundary. Macros embedding hash literals work; macros with mid-arg comments do NOT (write the comment after whitespace).
  - **No string escape processing.** `"\X"` is 2 literal bytes (`\`, `X`); `\"` closes the string. If escape is needed later, change `readString` AND update `TestStringHasNoEscapeProcessing` (the test name encodes the contract).
  - **Mixed-case macros become `UnknownStmt`, not errors.** Permissive-parsing-of-unknowns lets us ignore macros we haven't taught the rule emitter yet (e.g. `RECURSE_FOR_TESTS`, `NO_LINT`). Adding a new typed Stmt later just means flipping that name from `UnknownStmt` to a typed branch.
  - **`SET` arity is enforced as exactly 2.** `SET(only_one)` errors. Real ya.make may have `SET(NAME val1 val2)` for list values — if encountered, change to permissive arity and let the consuming rule decide what to do with the args.
  - **Optional on-disk smoke test (`TestParseArchiverYaMakeOnDisk`) only pins archiver, not library.** If `library/cpp/archive/ya.make` upstream drifts, the inlined library fixture will silently diverge. Acceptable nit-tier coverage gap.

- **PR-23 (PR-PIVOT)** (2026-05-07) — **M2 architectural keystone.** Replaces module addressing from "string path" → `ModuleInstance{Path, Language, Target PlatformID, Flags FlagSet}` per D30. Rule signatures take `instance ModuleInstance` (D33). `genCtx.memo: map[ModuleInstance]*moduleEmitResult` (D34). `(ModuleInstance).WithHost(cfg)` flips Target to host PlatformID + sets `Flags.PIC=true` (D31 cross-platform recursion mechanism — wiring for PR-25). New `module.go` defines all addressing types. New `toolchain.go` defines `PlatformConfig{Target, Host PlatformSpec}` + `DefaultLinuxConfig`. `Emitter.OnReady(NodeRef) <-chan struct{}` interface added (D37 early-execution contract); BufferedEmitter shared-channel no-op impl. `cc.go` rewritten with three composers: target (101 args), host (105 args, NEW — release-flavoured `-O3 -fPIC -DNDEBUG`), musl (111 args). Output paths `.o` ↔ `.pic.o`, `.a` ↔ `.pic.a` driven by `Flags.PIC`. `r6.go` rewritten — takes `ragel6LD NodeRef` parameter, wires via `ForeignDepRefs["tool"]` (NO stub UID). `as.go` signature updated to take `yasmLD *NodeRef` (after D01 fix — pointer, not bool sentinel). Cherry-picks: PR-13 yamake.go/macros.go (parser AST + macro evaluator + INCLUDE cycle detection), PR-14 musl flag bundles (ModuleFlavor type DISCARDED — subsumed by Flags), PR-15 ar.go (signature updated to instance), PR-17 cp.go/js.go (signature updated). Files: `module.go`, `cp.go`, `js.go`, `r6.go`, `macros.go` (5 new); `gen.go`, `toolchain.go`, `cc.go`, `ar.go`, `as.go`, `flags.go`, `yamake.go`, `emitter.go` modified. Plus all corresponding `_test.go`. Verification: `go build/vet/gofmt` clean; full suite PASSES; M1 regression preserved (2 matched / 2 pairs at L1/L2/L3); host CC byte-exact (105 args for `lib.c.pic.o`); host AR byte-exact for `build/cow/on`; ModuleInstance equality + WithHost verified.
  Two review rounds. Round 1 (5 defects, 0 major):
  - **D01 (minor)** — EmitAS signature deviated with `(yasmLD NodeRef, hasYasm bool)` instead of `(yasmLD NodeRef)`. **Fixed**: replaced with `(yasmLD *NodeRef)` (nil = no yasm). Cleaner type-level distinction.
  - **D02 (nit)** — stale `noYasmRef` doc reference. **Fixed**: replaced with "pass `nil` for yasmLD".
  - **D03 (nit)** — module.go header said `Extra []string` but actual field is `string`. **Fixed**: header updated to "`Extra` is `\n`-joined sorted concatenation (string, not []string, because slice fields disqualify struct as map key per D34)".
  - **D04 (nit)** — `peerArchivePaths` allocated but never read in genModule. **Fixed**: dead code deleted.
  - **D05 (nit)** — Emitter.OnReady doc lacked per-ref-channel note for M3 implementer. **Fixed**: doc extended with "BufferedEmitter shared channel; StreamingEmitter MUST switch to per-NodeRef channels."
  Round 2: NO NEW DEFECTS. All 5 fixes verified.
  Surprises / constraints future PRs must respect:
  - **`FlagSet.Extra` is `string` (not `[]string`).** Slice fields disqualify struct as map key. Constructor `NewFlagSet(extra ...string)` sorts and `\n`-joins. Reviewer must NOT "DRY it up" to `[]string` without re-architecting memo keys.
  - **`ModuleInstance` is comparable by value.** Map-keyable. Used in `genCtx.memo`. New fields added later MUST also be comparable (no slice/map/func types).
  - **`(ModuleInstance).WithHost(cfg)`** flips `Target` to host PlatformID and sets `Flags.PIC=true`. Path/Language preserved. PR-25 will use this in genModule when encountering host-tool deps.
  - **`Emitter.OnReady(NodeRef) <-chan struct{}` is interface.** BufferedEmitter shared channel closes at Finalize (no-op for this impl). M3 StreamingEmitter MUST close per-ref channels as deps resolve. Doc captures this constraint.
  - **`Finalize` STILL rejects pre-populated `node.ForeignDeps`.** PR-17's relax never landed; cross-platform recursion (D31) feeds real refs via `ForeignDepRefs`, not literal strings.
  - **`EmitAS(instance, srcRel, includes []string, yasmLD *NodeRef, emit)`** — signature has `*NodeRef` for optional yasm dep + `includes []string` (PR-16 inheritance for per-module ADDINCL). PR-25's macro integration may further refine `includes` source.
  - **`EmitR6(instance, srcRel, ragel6LD NodeRef, emit)`** — caller MUST provide a real ragel6 host LD ref. Today PR-23 doesn't wire this in genModule (only synthetic test exercises it); PR-25 will plumb it.
  - **`Gen(cfg PlatformConfig, sourceRoot, targetDir string)`** STILL takes `cfg` (now full `PlatformConfig{Target, Host}`) and `targetDir` (for backward compat with existing CLI). The recursive walker constructs ModuleInstances internally via `inferFlagsFromPath(path, isPIC) FlagSet`. Direct API for advanced users is `genModule(ctx, instance ModuleInstance) *moduleEmitResult`.
  - **PR-13/14/15/17 worktrees retained** as cherry-pick history. They will not be merged. Runtime auto-cleans when branches are GC'd.
  - **Macro evaluator (PR-13's `EvalCond`) is in code but UNCALLED** by gen.go — that's PR-25's wiring job. Hitting an `IfStmt` in gen.go's switch fires the "unhandled Stmt type" throw.
  - **Cross-platform recursion mechanism (D31) is in PLACE but UNTRIGGERED** by gen.go's walker. PR-25 wires it up. Until then, build/cow/on Gen returns 2 nodes (target only); host instances are exercised only by direct test calls.

- **PR-24** (2026-05-07) — `ld.go` (4-cmd LD rule per D22) + Gen wiring for PROGRAM modules. `EmitLD(instance, ccRefs, ccPaths, peerLDRefs, peerLibPaths, pluginRefs, pluginPaths, globalRefs, globalPaths, emit) NodeRef` produces the 4-cmd LD Node verified byte-exact against `tools/archiver/archiver` reference: cmd[0] vcs_info.py (5 args), cmd[1] clang compile of __vcs_version__.c (94 args), cmd[2] link_exe.py (73 args; libs in PEERDIR walk order — non-alphabetical R14; cwd=$(BUILD_ROOT)), cmd[3] fs_tools.py link_or_copy_to_dir (5 args). gen.go: PROGRAM modules dispatch to EmitLD instead of EmitAR; result.LDRef + result.ARRef both point at the LD node for back-compat. PROGRAM-as-peer rejection (PROGRAMs are binaries, not linkable libraries). New `node.go` field: `Cmd.Cwd string` with `omitempty` (reference LD's cmd[2] + 58/83 AS nodes carry cwd; field added structurally). `compare_cmd.go` l3Match extended to compare Cwd. `as.go` updated: EmitAS now sets `Cwd: "$(BUILD_ROOT)"` unconditionally (chkstk reference confirmed). Files added: `ld.go`, `ld_test.go`. Modified: `gen.go`, `gen_test.go`, `node.go`, `compare_cmd.go`, `as.go`, `as_test.go`. Verification: `go build/vet/gofmt` clean; full suite PASS; M1 regression preserved (2/2 at L1/L2/L3); `TestEmitLD_ToolsArchiver_ByteExact` PASSES (all 4 cmds + env + cwd + 10-input non-alphabetical order + outputs + kv + DepRefs count=35).
  Two review rounds (6+0 defects, 0 major):
  - **D01 (minor)** — AS emitter didn't set Cwd; reference has cwd on 58/83 AS nodes. **Fixed**: AS sets `Cwd: "$(BUILD_ROOT)"` unconditionally; chkstk byte-exact still passes.
  - **D02 (minor)** — PROGRAM-as-peer rejection had no test. **Fixed**: `TestGen_RejectsProgramAsPeer` added.
  - **D03 (minor)** — EmitLD length-mismatch checks for peerLD/plugin/global untested. **Fixed**: `TestEmitLD_LengthMismatchPanics` expanded to 4 table-driven subtests.
  - **D04 (nit)** — stale comment "3 CC + 3 AR" in TestGen_PeerdirDeclarationOrder_Preserved. **Fixed**: updated to "3 CC + 2 AR + 1 LD".
  - **D05 (nit)** — `cap` shadow in composeLDCmdLinkExe. **Fixed**: renamed to `argCap`.
  - **D06 (nit, deferred)** — EmitLD plugin/global path-prefix convention asymmetric. Documented; PR-25's macro evaluator is the first consumer.
  Surprises / constraints future PRs must respect:
  - **`Cmd.Cwd` field added to node.go** with `omitempty`. Existing CC/AR/CP/JS/R6 emitters don't set it; their reference nodes don't have it; UID-stability preserved (omitempty omits empty string from JSON). LD's cmd[2] sets `$(BUILD_ROOT)`. AS sets `$(BUILD_ROOT)` UNCONDITIONALLY post-D01 fix.
  - **AS-Cwd asymmetry will surface in PR-25/26**: 25 of 83 reference AS nodes lack cwd. ALL 25 are in `contrib/libs/asmlib/`. NOT in build/cow/on closure. When PR-25/PR-26's wider walk hits asmlib, those 25 nodes will diverge at L3 (reference cwd empty; ours `$(BUILD_ROOT)`). Fix path: make Cwd conditional on per-module flag OR per-source heuristic. Constraint logged for PR-25/PR-26 reviewer.
  - **EmitLD signature has 4 paired ref/path slice pairs** (cc, peerLD, plugin, global). pluginPaths are pre-`$(BUILD_ROOT)/`-prefixed (verbatim cmd_args slot); globalPaths are BUILD_ROOT-relative (cmd_args raw, inputs prepend prefix). Asymmetric convention — PR-25's macro evaluator must keep them straight or normalize.
  - **`EmitLD` REJECTS host (PIC=true)** — PR-24 ships only target LD. Host PROGRAMs not in archiver reference. `TestEmitLD_RejectsHostPIC` pins. Future host LD removes the guard explicitly.
  - **PROGRAM modules emit LD as their result**, not AR. result.LDRef points to LD; result.ARRef = result.LDRef for back-compat (gen test asserts on Result UID via either). LIBRARY modules unchanged (still emit AR).
  - **PROGRAM-as-peer is a hard error.** A PROGRAM is a binary; cannot be PEERDIRed. Caught in genModule with concrete diagnostic.

- **PR-25** (2026-05-07) — **M2 Wave-3 keystone.** Walker integrates macro evaluator + per-instance flag derivation + host-tool recursion. genModule walks PEERDIR closure passing parent ModuleInstance forward. `collectStmts` evaluates IF/ELSE via EvalCond + DefaultIfEnv (per-platform); collects per-module data (srcs, joinSrcs, globalSrcs, addIncl, cFlags, cxxFlags, cOnlyFlags, ldFlags, srcDir). NO_LIBC/NO_UTIL/NO_RUNTIME/NO_PLATFORM/NO_COMPILER_WARNINGS overlay onto FlagSet. Source dispatch by extension: .c/.cpp/.cc/.cxx → EmitCC; .S/.s → EmitAS (with optional yasm host LD via WithHost recursion); .rl6 → EmitR6 (host ragel6 LD via WithHost) + EmitCC of generated .cpp; JOIN_SRCS → EmitJS + EmitCC. GLOBAL_SRCS → second AR with module_tag=global; PROGRAM peer GlobalRef threaded into EmitLD's globalRefs/globalPaths slots (per D08 fix: BUILD_ROOT-relative). Headers (.h/.hpp) skipped. Whitelist of metadata macros extended (whitelistedMetadataMacros). Host PIC LD guard removed (D31 cross-platform recursion now triggers host PROGRAMs for ragel6/yasm). Files: `gen.go` (large rewrite), `gen_test.go` (6 new tests), `ld.go` (host-PIC guard removed), `ld_test.go` (TestEmitLD_RejectsHostPIC → TestEmitLD_AcceptsHostPIC with HostPlatform/Tags assertions). Verification: `go build/vet/gofmt` clean; full suite PASS; M1 regression preserved (2/2 at L1/L2/L3); `./yatool gen --target tools/archiver` rc=0, 50 nodes emitted; `compare` reports L1=50/50 byte-exact (every emitted node pairs at L1), L2=49/50, L3=12/50, L0=46/3730 (1.23%).
  Three review rounds. Round 1 (7 defects, 0 major): D01 CXXFLAGS/CONLYFLAGS silently dropped; D02 SRCDIR collected but not consumed; D03 peer global archive dropped from PROGRAM's LD; D04 TestGen_ToolsArchiver_DoesNotCrash too lax; D05 stale var name pr12SupportedUnknownMacros; D06 TestEmitLD_AcceptsHostPIC under-asserts; D07 R6 .cpp inputPath uses SOURCE_ROOT for BUILD_ROOT-located file. **All 7 fixed**: D01 collect into moduleData.cxxFlags/cOnlyFlags; D02 + D07 explicit deferral comments (PR-26); D03 moduleEmitResult.GlobalRef/GlobalPath + EmitLD threading + new TestGen_PeerGlobalArchive_ThreadsToLD; D04 t.Fatalf on exception + ≥50 nodes assert; D05 rename → whitelistedMetadataMacros; D06 HostPlatform/Tags assertions added.
  Round 2 (4 defects: 1 MAJOR, 3 nit): D08 GlobalPath had $(BUILD_ROOT)/ prefix violating EmitLD contract → double-prefixed inputs / malformed cmd_args (REGRESSION from D03 fix; missed because D03 test only checked refs/counts, not paths); D09 stale var name in test comment; D10 D07 comment cited wrong line range; D11 missing capacity hint. **All 4 fixed**: D08 dropped prefix + 3 regression-guard assertions in test (single-prefix in inputs, no double-prefix, unprefixed in cmd_args); D09 rename in comment; D10 rewrote without line refs; D11 capacity hints added.
  Round 3 (1 nit, deferred): D12 D10 rewrote with wrong scope ("this function" — actually different function). Deferred (search-for-EmitJS hint still works).
  Surprises / constraints future PRs must respect:
  - **Walker emits 50 of ~169 modules in tools/archiver closure.** The 119 missing modules are implicit DEFAULT_PEERDIRs added by upstream ymake (libcxx, libcxxrt, builtins, musl, malloc, abseil, util, etc.) that aren't in any explicit PEERDIR. PR-26 must hardcode these or derive from build/conf/. Without them, L3 stays at 12/50 ≈ 0.32% of full graph.
  - **CC for generated sources (JS/R6 outputs) uses SOURCE_ROOT inputPath** instead of BUILD_ROOT. Wrong path; would fail at execution. JSON-only today (no execution). PR-26 lands `EmitCCFromBuildRoot` variant + threads JS/R6 NodeRef into CC's DepRefs (UID derivation currently ignores generator content).
  - **ADDINCL/CFLAGS/CXXFLAGS/CONLYFLAGS/SRCDIR collected in moduleData but NOT consumed** by rule emitters. PR-26 wires them through (extends EmitCC signature OR passes via flavor.Extra).
  - **Peer global archive propagation** through PROGRAM PEERDIR-walk: `moduleEmitResult.GlobalRef` (optional) collected; PROGRAM aggregates direct-peer globals only — TRANSITIVE global propagation (when peer-of-peer has GLOBAL_SRCS) NOT yet handled. PR-26 work.
  - **Host PROGRAM LD bundle**: still uses target-flavored cmd_args. PR-26 needs hand-translated host LD bundle for ragel6/yasm host PROGRAMs (release-flavoured `-O3 -fPIC -DNDEBUG` link args).
  - **WithHost recursion only fires for .rl6 (R6) sources** — yasm wiring for .S sources is signature-ready but NOT triggered by the walker. AS sources for asmlib (which need yasm host LD) currently pass nil yasmLD. PR-26 wires this.
  - **whitelistedMetadataMacros is the single source of truth** for "macros that parse but have no rule effect today". When PR-26+ encounters a new metadata-only macro in PEERDIR closure, add it here. When a non-metadata macro is found (e.g. CONLYFLAGS was caught here as semantically active), do NOT add to whitelist — handle as typed Stmt or in applyUnknownStmt.

- **PR-26** (2026-05-07) — **Walker expansion via implicit DEFAULT_PEERDIRs.** `defaultPeerdirsFor(instance) []string` returns implicit peers ymake adds automatically: musl (suppressed by NO_LIBC), contrib/libs/cxxsupp/builtins (suppressed by NO_RUNTIME OR NO_PLATFORM), library/cpp/malloc/api (suppressed by NO_PLATFORM). Self-cycle prevention via path-component-aware `path == "X" || HasPrefix(path, "X/")` guards. `effectiveNoPlatform` synthesizes NO_PLATFORM from NoLibc+NoUtil+NoRuntime triple (preserves M1: build/cow/on declares triple via inferFlagsFromPath but no NO_PLATFORM macro). Defaults prepended to explicit peers in walker; de-duplicated via seen-map (per D03 fix). `peerYaMakeExists` filter discriminates ENOENT (silent skip for synthetic test trees) vs other errors (ThrowFmt for production-tree corruption per D05 fix). 3 new DefaultIfEnv bindings: USE_EAT_MY_DATA, ARCH_ARM6, WITH_MAPKIT (verified-needed via empirical test removal). Files: `gen.go`, `gen_test.go`, `macros.go`. Verification: `go build/vet/gofmt` clean; full suite PASS; M1 regression preserved (2/2 at L1/L2/L3); **`./yatool gen --target tools/archiver` rc=0, 1541 nodes** (was 50; ~30× expansion); compare reports L0=40.91% (1526/3730), L1=41.31% (1541 paired), L2=41.26%, L3=35.60%.
  Two review rounds. Round 1 (6 defects, 0 major): D01 musl self-guard prefix-substring matched musl_extra; D02 5 of 8 DefaultIfEnv bindings speculative (not consulted by walk); D03 explicit-PEERDIR-of-default produced duplicate refs (latent); D04 missing test cases (no_util_only, musl_extra_not_self); D05 peerYaMakeExists swallowed non-NotExist errors; D06 effectiveNoPlatform docstring conflated path-heuristic source with macro source. **All 6 fixed in round 2** + new TestGen_DefaultPeerdirs_ExplicitDuplicateDeduped. Round 2: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **Walker covers 1541/3730 nodes (41.3%) of full reference graph.** Remaining 2189 nodes blocked by parser gap: libcxx/libcxxrt/libunwind use `IF (X == "Y")` (string equality), util peers libc_compat which uses `IF (X < N)` (numeric `<`). PR-27 extends parser ADT with comparison operators.
  - **defaultPeerdirsFor returns 3 paths max** (musl, builtins, malloc/api). PR-27 adds libcxx/libcxxrt/libunwind/util to the set after parser extension lands.
  - **DefaultIfEnv discipline**: only add bindings that are actually consulted by the walk. Speculative additions get caught in review (per D02). The "unknown IF identifier" throw is the signal for new bindings.
  - **De-dup ordering matters**: defaults FIRST, then explicit. Reverse ordering would break the `isDefault := i < len(defaults)` boundary check elsewhere.
  - **peerYaMakeExists filter only applies to defaults**, not explicit peers. Explicit PEERDIRs throw on missing — that's a real error. Synthetic test trees can omit musl/builtins/malloc/api ya.makes without breaking.
  - **L1=L0 (1541 vs 1526)**: there's a 15-node L0/L1 discrepancy because L1's pairing-by-(outputs[0], platform) catches some nodes that L0's fingerprint topology missed. Investigate when relevant.
  - **L3=35.60% on 1541 nodes** = ~544 nodes byte-exact. Most musl CC nodes are byte-exact (PR-14's musl bundle is correct). Mismatches are likely cxxsupp/builtins (separate flag bundle) and modules with ADDINCL/CFLAGS not yet threaded (PR-28 work).

- **PR-27** (2026-05-07) — **Parser comparison operators + libcxx/util walks.** `yamake.go` AST extended with `ExprString`, `ExprInt`, `ExprEq`, `ExprLt` (plus lexer tokens `tokInt`, `tokEq`, `tokLt`, `tokNotEq`); `parseCmp` desugars `A != B` → `ExprNot{ExprEq{A,B}}`. `macros.go` `DefaultIfEnv` becomes typed `Environment{bools, strings, ints}` with new bindings: `CXX_RT="libcxxrt"`, `SANITIZER_TYPE=""`, `ANDROID_API=0`, plus literal/false bindings (`undefined`/`memory`/`address`/`thread`/`leak`, `OS_FREEBSD`/`OS_CYGWIN`/`SUN`/`CYGWIN`/`ARCH_WASM32`/`ARCH_WASM64`/`FUZZING`/`EXPORT_CMAKE`/`NO_CXX_RTTI`/`NO_CXX_EXCEPTIONS`/`USE_ARCADIA_COMPILER_RUNTIME`/various `PROVIDE_*`). `gen.go` `defaultPeerdirsFor` extended with `libcxx`, `libcxxrt`, `libunwind` (suppressed by NO_PLATFORM) and `util` (suppressed by NO_UTIL); `runtimeAncestorPaths` set added to short-circuit self-recursion when a runtime-ancestor module would re-enter itself; cycle handler relaxed throw → tolerate-with-stderr-diagnostic + `ctx.cyclesTolerated` counter (D02 fix); ragel6 host recursion `Try` narrowed to `*ParseError` only (D03 fix); header-only LIBRARY tolerance added. Files: `yamake.go`, `macros.go`, `gen.go`, plus tests. Verification: `go build/vet/gofmt` clean; full suite PASS; M1 regression preserved (2/2 byte-exact L0/L1/L2/L3); **`./yatool gen --target tools/archiver` rc=0, 1696 nodes** (was 1541; +155); compare reports L0=44.91%, L1=45.39%, L2=44.37%, L3=35.84%. Production observation: `gen: PEERDIR cycle tolerated at util` prints 7× per archiver build — one per `util`-reaching consumer that hits the implicit-default cycle.
  Two review rounds. Round 1 (6 defects, 2 major): D01 coverage 1696 < target 2500 (DEFERRED — structural dual-platform gap, PR-28+); D02 cycle handler silent-relaxation lacked diagnostic; D03 ragel6 `Try` over-broad (caught any exception type); D04 per-path self-suppression checks dead given `runtimeAncestorPaths` early-exit; D05 stale `SANITIZER` mention in DefaultIfEnv doc; D06 negative integer literals lex as tokWord (DEFERRED — closure does not need it; documented as ExprInt invariant). **D02-D05 fixed in round 2; D01 + D06 deferred with explicit ledger entries.** Round 2: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **Coverage stalls at 1696/3730 (45.5%)** — two structural blockers dominate the gap: (a) walker emits `instance.Target` only, no general dual-platform pass; reference is dual-platform → ~1797 host nodes never emitted (~48% of total); (b) ragel6/yasm host-tool ya.makes use `INCLUDE(${ARCADIA_ROOT}/...)` which our parser does not expand → ~200 host-tool nodes lost. PR-28+ tackles dual-platform emission as a dedicated PR; INCLUDE-substitution gap is a separate parser extension.
  - **Cycle handler is now tolerate-with-diagnostic, not throw.** Empirically: every tools/archiver build tolerates 7 cycles, all `util` re-entered via `zlib`'s default-peer set. Safe at L0–L3 because all 48 reference AR nodes have zero peer-archive inputs (verified). Counter exposed via `ctx.cyclesTolerated`; future PR-28+ may want first-occurrence-only or summary-at-end pattern if closures grow and stderr noise becomes a problem.
  - **`runtimeAncestorPaths` is a hardcoded set** (musl, libc_compat, linuxvdso/, cxxsupp/{builtins,libcxx,libcxxrt,libcxxabi,libcxxabi-parts}, libunwind, malloc/api, sanitizer/include, util). Upstream ymake derives this from build/conf; we hand-translate. New runtime-ancestor modules added in future PRs MUST be added here, otherwise their default-peer expansion will throw cycles or emit malformed graphs.
  - **Per-path self-suppression checks in `defaultPeerdirsFor` are redundant defense-in-depth** — covered by `runtimeAncestorPaths` early-exit (PR-27-D04). Invariant comment at gen.go:478-484 documents this. Do NOT remove the per-path guards; the comment is the contract.
  - **Ragel6 host recursion `Try` swallows ONLY `*ParseError`** — other exception types re-panic (D03 fix). If a future regression in `WithHost`/`derivePeerInstance` introduces a non-parse exception in host-instance walking, it surfaces loudly instead of producing zero `ragelLDRef` and L3 byte-exact divergence on every `.rl6` module.
  - **Typed `Environment{bools, strings, ints}`** is the standard going forward. Bool() helper handles ident-typed lookups. Lookup precedence: strings → ints → bools. Bindings ONLY justified by an actual `IF` consultation in the walked closure — speculative bindings get caught in review (PR-26-D02 precedent).
  - **Negative integer literals are unsupported** by the current lexer (`-N` lexes as `tokWord`). Documented as ExprInt invariant; defer the lexer extension until a real closure ya.make needs it.
  - **15 reference module_dirs entirely missing** from current output: asmlib, asmglibc, jemalloc, mimalloc, tcmalloc/{malloc_extension,no_percpu_cache}, abseil-cpp, ragel6 host, yasm host, malloc/{mimalloc,tcmalloc}, musl/full, musl/include, musl_extra. These are reached only via ALLOCATOR_IMPL/EXTRALIBS/RECURSE which PR-27 keeps as metadata-only. PR-28+ dual-platform pass + ALLOCATOR_IMPL routing both needed.
  - **L2=44.37% < L1=45.39%** — small drop from L1 to L2 (~1%). L1's pairing catches outputs that L2's input/tag/requirements compare doesn't pin. Investigate when relevant.

- **PR-28** (2026-05-07) — **Dual-platform emission via demand-driven host PROGRAM closure walk.** Plan: `docs/drafts/20260507-1131-pr28-dual-platform.md`. Model (c): host emission rooted at host PROGRAMs (ragel6, yasm); BFS over PEERDIR closure under host instance covers exactly all 1797 reference host nodes. Demand-driven (model option (ii)): host walk fires only when target source enumeration discovers `.rl6` (→ ragel6) or asmlib `.S` (→ yasm). Eager recursion at trigger sites — the (now removed) two-phase drain machinery turned out to be dead. PROGRAM(name) honoring landed: `EmitLD(instance, binaryName, ...)` derives binary from `ModuleStmt.Args[0]`; EmitR6 takes ragel6BinaryPath threaded from host LD's emitted output. SRCDIR threading: emitOneSource takes srcDir; per-source CC/AS/R6 nodes rebase to `<sourceRoot>/<srcDir>`; JOIN_SRCS branch constructs srcInstance and threads to EmitJS + downstream EmitCC; EmitJS path composition fixed (was bare-filename `$(SOURCE_ROOT)/<file>`). ALLOCATOR macro resolution (D12 of plan): table-driven SYM→peerdir mapping; ALLOCATOR(SYSTEM) unconditionally adds library/cpp/malloc/system per ymake.core.conf:1038-1040. R6 deps inversion fix: ragel6LD now in DepRefs (was ForeignDepRefs["tool"]). util-target-only gate via `instance.Target == cfg.Target.ID` (after D08 fix from PIC-flag misuse). New macro binding: none retained (DISABLE_INSTRUCTION_SETS was speculative; removed empirically per round-2 verification). Files: `gen.go` (large rewrite), `r6.go`, `js.go`, `ld.go`, `macros.go`; tests `gen_test.go`, `r6_test.go`, `ld_test.go`, `js_test.go`. Verification: `go build/vet/gofmt` clean; full suite PASS; M1 regression preserved (2/2 byte-exact L0/L1/L2/L3); **`./yatool gen --target tools/archiver` rc=0, 3321 nodes** (1739 target + 1582 host; was 1696 target / 0 host); compare reports L0=88.34% (3295/3730), L1=88.66% (3307 paired), L2=86.89%, L3=36.46%. Cycles tolerated: 7 (target only — host-axis util gate prevents host-flavor cycles).
  Three review rounds. Round 1 (10 defects, 2 major): D01 R6 cmd_args path mismatched our own host LD output (functionally invalid graph); D02 host ragel6 module_dir = /bin instead of parent (10 nodes wrong); D03 drainHostToolWalks dead scaffolding; D04 DISABLE_INSTRUCTION_SETS dead binding; D05 ALLOCATOR(SYSTEM) silently dropped malloc/system; D06 yasm/asmlib end-to-end dead in M2 closure; D07 host-count test floor too loose; D08 PIC-as-axis-discriminator wrong; D09 R6 test missing path-consistency assertion; D10 plan-doc not in worktree (process artifact, not real defect). **D01-D09 fixed in round 2; D10 invalidated.** Round 2 (3 minor): D11 SRCDIR threading missed JOIN_SRCS; D12 D02 acceptance criterion regression test missing; D13 stale comment in collectStmts. **All 3 fixed in round 3.** Round 3: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **Host emission is demand-driven, model (c).** When target enumeration encounters `.rl6` source, host ragel6 LD is fetched via `WithHost(cfg)` recursion; when asmlib `.S` is encountered, host yasm LD is fetched. The eager-recursion model means the trigger sites get the host LD ref inline, NOT via a deferred drain. Do NOT reintroduce a two-phase drain — it was tried, removed (D03), and the eager model works.
  - **EmitLD honors PROGRAM(name)** via `binaryName` parameter. Empty falls back to `lastPathComponent(instance.Path)`. All callers MUST pass binaryName when the parsed module declares `PROGRAM(<name>)`. Future rule emitters that compose binary paths from instance.Path (e.g. tool wrappers in M3) MUST consume binaryName, not lastPathComponent — failing this produces functionally invalid graphs (PR-28-D01 was exactly this bug).
  - **EmitR6 takes ragel6BinaryPath from caller**. Caller passes `result.LDPath` from the host walk. Internal-consistency invariant pinned by TestGen_HostToolRecursion_R6 (D09): `r6Node.Cmds[0].CmdArgs[0] == hostRagel6LD.Outputs[0]`. Future host-tool wrappers MUST follow the same pattern (path threaded from consumed host LD output) — do NOT hardcode tool paths.
  - **SRCDIR threading touches: per-source emit (CC/AS/R6) AND JOIN_SRCS branch (EmitJS + downstream EmitCC).** LD/AR remain at instance.Path (semantic difference: binary/archive lives where declared, even if sources are elsewhere; reference confirms this). EmitJS's path composition is now `<instance.Path>/<src>` for cmd_args and `$(SOURCE_ROOT)/<instance.Path>/<src>` for inputs — fixes preexisting bare-filename bug.
  - **util is target-only implicit peer.** Reference graph emits zero host nodes under util/. Suppression via `instance.Target == cfg.Target.ID` predicate in defaultPeerdirsFor. The proper upstream rule is `_BUILTIN_PEERDIR`'s USE_CXX/NO_UTIL gating; the target-axis check approximates it for M2. If a future host PROGRAM legitimately needs util, this gate will under-emit — replace with the real upstream rule then.
  - **Cycle tolerance count: 7 (unchanged from PR-27)** — all `util` re-entered via `zlib`'s default-peer set on target axis. Host-axis util gate prevents additional host-flavor cycles. The cycle handler still emits stderr diagnostic per cycle; if the count grows, investigate root cause before raising the threshold.
  - **17 reference module_dirs entirely missing from current output (after PR-28):** asmlib (27 host nodes), asmglibc, jemalloc, mimalloc, tcmalloc/{malloc_extension,no_percpu_cache}, abseil-cpp, malloc/{mimalloc,tcmalloc}, musl/full, musl/include, musl_extra, plus the host yasm closure (8 nodes). All blocked by either (a) musl/full closure path not modelled (`_BUILTIN_PEERDIR`'s MUSL=yes && !MUSL_LITE → musl/full at ymake.core.conf:1238-1245), or (b) ALLOCATOR_IMPL/EXTRALIBS/RECURSE routing not modelled. PR-29+ targets these.
  - **L1 vs L2 lift differential after PR-28**: L1 (88.66%) > L2 (86.89%) > L3 (36.46%). The L2-L3 gap is dominated by the pre-existing "CC for generated sources uses SOURCE_ROOT inputPath instead of BUILD_ROOT" (tasks.md:223 / PR-25 known-deferred), which makes JS-derived CC nodes pair at L1 but diverge at L2/L3. PR-29's EmitCCFromBuildRoot work closes this.
  - **PR-28 forward-scaffolding kept**: yasm/asmlib trigger code at gen.go .S branch + TestGen_HostWalk_AsmlibYasmWired stay in place even though the M2 closure doesn't reach them. Removing would force PR-29/30/31 to re-derive the wiring when the musl/full closure path lands.
  - **defaultPeerdirsFor signature now takes `*genCtx`** (D08 fix). Passes nil-safe fallback to `DefaultLinuxConfig.Target.ID` for unit tests. Future per-platform default rules MUST consult ctx for platform-axis decisions, not flag-level signals.

- **PR-29** (2026-05-07) — **L3 lift via host-musl CC bundle + per-module flag threading.** Plan: `docs/drafts/20260507-1300-pr29-flag-threading.md`. **Headline: L3 36.46% → 80.05% (+43.6pp; M2 acceptance gate ≥50% cleared with 30pp headroom).** Lead deliverable D01: `composeMuslHostCC` 115-arg bundle for host-PIC musl modules (1297 nodes, dominant lever). Plus D02: per-module CXXFLAGS/CONLYFLAGS via `ModuleCCInputs.CXXFlags`/`COnlyFlags` (source-language dispatch — `.cpp/.cc/.cxx` → CXXFlags, `.c` → COnlyFlags); D03: per-module ADDINCL via `ModuleCCInputs.AddIncl` slotted between ccIncludes prefix (BUILD_ROOT/SOURCE_ROOT) and suffix (linux-headers); D05: `pickCompiler(isCxx)` dispatches clang vs clang++ + cxxStandardFlag (-std=c++20) for C++ sources; D06: `pickWarningFlags(NoCompilerWarnings)` substitutes muslWarningFlags; D07: `IsGenerated` bool switches inputPath from $(SOURCE_ROOT) to $(BUILD_ROOT) (Generator → DepRefs deferred to PR-30 due to L0 cost cascade — 24 fingerprint flips would cost 2 L0 matches). EmitCC signature pivoted to `EmitCC(instance, srcRel, in ModuleCCInputs, emit) (NodeRef, string)` per plan recommendation (b) — forward-compatible struct extension. **Round-1 MAJOR defect D01: CXXFLAGS GLOBAL leaked as literal cmd_arg in 116 libcxx CC nodes** (applyUnknownStmt didn't strip GLOBAL modifier); **fixed in round 2 via splitGlobalModifier**. D02-D05 round-1 minors (test coverage gap, doc honesty, latent musl C++ duplicate warning, unused param) all fixed. Files: `cc.go` (large rewrite), `flags.go` (split ccIncludes prefix/suffix; new `cxxCompilerPath`, `cxxStandardFlag`, `muslCcIncludesX8664`), `gen.go` (ModuleCCInputs threading + GLOBAL filter in applyUnknownStmt), tests (`cc_test.go` 8 new pin tests + `gen_test.go` GLOBAL-leak regression). Verification: `go build/vet/gofmt` clean; full suite PASS; M1 regression preserved (2/2 byte-exact L0/L1/L2/L3); tools/archiver 1696→3321 nodes unchanged from PR-28 (PR-29 doesn't add nodes); compare reports L0=88.34% (preserved), L1=88.66% (preserved), L2=87.53% (was 86.89%, +0.64pp from D07 inputPath fix), **L3=80.05%** (was 36.46%, +43.6pp). Cycles: 7 (unchanged).
  Two review rounds. Round 1 (5 defects, 1 major): D01 GLOBAL leak (functionally invalid graph), D02 test coverage bypassed parser→walker→emit pipeline, D03 cc.go Generator field comment dishonest about deferral, D04 latent double `-Wno-everything` injection for future musl C++, D05 unused noCompilerWarnings param in musl composers. **All 5 fixed in round 2.** Round 2: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **`ModuleCCInputs` struct is the EmitCC extension point.** Adding new flag classes (e.g. PR-30's peer-propagated GLOBAL CXXFLAGS via `cxxFlagsGlobal`) appends a field rather than changing signature. Forward-compatible. All four composers (target/host/musl/musl-host) consume the struct uniformly except where empirically justified (musl drops own ADDINCL because muslCcIncludes already encodes them; D04 PR-30 will add Generator wiring once L0 cost can be absorbed).
  - **GLOBAL CXXFLAGS/CONLYFLAGS are DROPPED at applyUnknownStmt** today (PR-29-D01 fix). PR-30's D04 must route GLOBAL-prefixed args through a NEW `cxxFlagsGlobal`/`cOnlyFlagsGlobal` field on moduleData/moduleEmitResult and propagate via PEERDIR walk. Don't reintroduce direct append-to-self for GLOBAL — that was the bug.
  - **applyUnknownStmt for ADDINCL/CFLAGS already calls splitGlobalModifier** (yamake.go:863, :867) via typed *AddInclStmt/*CFlagsStmt. CXXFLAGS/CONLYFLAGS use untyped UnknownStmt; PR-30 may want to introduce typed *CXXFlagsStmt/*CONLYFlagsStmt for symmetry, OR keep them untyped and just use splitGlobalModifier in applyUnknownStmt. Pick whichever fits PR-30's GLOBAL-routing design.
  - **Musl composer drops per-module ADDINCL at composer entry** (deviation from plan's uniform threading). Empirically: musl ya.make ADDINCL paths exactly mirror `muslCcIncludes`/`muslCcIncludesX8664` — threading them produces 121-arg double-injection. Footgun: if a future musl module declares an ADDINCL path NOT in the bundle, it gets silently dropped. Acceptable for M2 but consider tighter assert (e.g. throw on unknown musl ADDINCL path) in M5 hardening.
  - **EmitCC's `Generator NodeRef` field is forward-scaffolding** (PR-29-D03). No caller sets it; gate at cc.go:202 is intentionally dead. PR-30 will wire `gen.go`'s JS/R6 branches to set `ccIn.Generator = jsRef` (or r6Ref), at which point the gate fires and the CC node's DepRefs gain the generator dep. PR-30 must verify the L0 cost cascade (round-1 deferral noted 2 multiset matches lost) is recoverable via concurrent fixes elsewhere (e.g. ALLOCATOR_IMPL closure widening fingerprint pool).
  - **`appendCxxStdAndOwn` takes `injectCxxWarningBundle bool`** (D04 fix). Musl composers pass false (already added warning bundle); non-musl pass true. PR-30/31 introducing new composers (e.g. for libcxxrt host bundle) MUST decide carefully whether the helper's warning-bundle injection conflicts with composer-already-appended bundles. Pattern: if composer appends warningFlags before calling appendCxxStdAndOwn, pass false.
  - **L3 80.05% / 660 remaining divergent pairs**: dominant remaining surface is libcxx + libcxxrt + util C++ consumers blocked by D04 PR-30 (peer-propagated GLOBAL `-nostdinc++`, GLOBAL `-DLIBCXXRT`, GLOBAL `-D_musl_=1` CFLAG, cxx-warning-extension peer bundle), plus AS-source divergence (56 paired pairs not byte-exact). PR-30 closes these.
  - **L2 87.53% / 42 divergent pairs**: L2 measures inputs/tags/requirements equality. PR-29 D07 fixed 24 JS-derived CC node inputPath ($(SOURCE_ROOT) → $(BUILD_ROOT)). Remaining 42 pairs likely have different DepRefs structure (will improve when PR-30 wires Generator → DepRefs).
  - **CXXFLAGS GLOBAL test pattern**: TestGen_CXXFLAGS_GLOBAL_NotLeakedToOwnCmdArgs constructs synthetic ya.make → Gen → assert no `"GLOBAL"` literal in cmd_args. Reuse this pattern for any future GLOBAL-modifier-bearing macro in PR-30 (CFLAGS GLOBAL, CXXFLAGS GLOBAL, CONLYFLAGS GLOBAL).
  - **EmitCC's signature pivot is invasive**: 5 call sites in gen.go updated. Future rule emitters that call EmitCC (e.g. M3 streaming variants) MUST construct ModuleCCInputs explicitly. The zero-value behaves like the pre-PR-29 base composer (no flag threading) — convenient default for tests.

- **PR-30** (2026-05-07) — **M2 L0 acceptance closer.** Plan: `docs/drafts/20260507-1404-pr30-l0-closer.md`. **Headline: L0 88.34% → 98.77% (+10.43pp; M2 acceptance gate ≥95% cleared with 3.77pp headroom). M2 ALL FOUR GATES MET.** L1 88.66% → 98.74%; L2 87.53% → 97.56%; L3 80.05% → 80.35%. M1 byte-exact preserved (2/2). Cycles: 7 (unchanged, ≤14 budget). 8 D-tasks shipped: D01 MUSL_LITE binding (+ opportunistic OPENSOURCE_REPLACE_LINUX_HEADERS / NO_CLANG_COVERAGE / NO_PROFILE_RUNTIME); D02 musl/full PROGRAM closure via `defaultProgramPeerdirsFor` + `ENABLE(MUSL_LITE)` per-module routing through applyUnknownStmt (cycle-breaker for yasm host PROGRAM); D03 PROGRAM-default ALLOCATOR routing (TCMALLOC_TC default; explicit-allocator-suppresses-default test pin); D04 Generator → DepRefs wiring via `HasGenerator` boolean (replaces zero-NodeRef check which had structural collision with BufferedEmitter id=0); D05 LIBRARY AR drops peer-archive deps (every reference AR has zero AR-on-AR deps verified empirically); D06 SRCDIR PROGRAM-only ancestor-rebase (LIBRARY with SRCDIR keeps module_dir + uses `__/<rel>` infix via composeCCPaths with stat-based local-vs-SRCDIR resolver); D07 yasmLD threaded into BOTH DepRefs AND ForeignDepRefs["tool"] + `.asm` extension dispatch in EmitAS (asmlib uses .asm not .S); D08 9 new tests (Generator JS, Generator R6, AR-no-peer, PROGRAM-default-musl-full, PROGRAM-default-tcmalloc, PROGRAM-explicit-allocator-no-default, srcdir-sibling-keeps-module-dir, srcdir-local-ignored, L0 ≥ 95% acceptance hook + as_test.go's TestEmitAS_YasmLD_PopulatesDepRefs from round 2). Files: `gen.go` (large rewrite), `cc.go` (composeCCPaths SRCDIR-aware composer + HasGenerator gate), `as.go` (yasm DepRefs + .asm dispatch), `ar.go` (docstring updates), `macros.go` (MUSL_LITE + opportunistic bindings), tests + `.gitignore`. Verification: `go build/vet/gofmt` clean; full suite PASS; tools/archiver 3321 → 3718 nodes (target 1739→1985 +246; host 1582→1733 +151); reference 3730. Of remaining 12 unaccounted: 35 unpaired-want + 47 unpaired-got at L1 (12 net spurious).
  Two review rounds. Round 1 (5 defects, 0 major): D01 ar.go peerArchiveRefs docstring outdated; D02 as.go top-level docstring missing DepRefs wiring; D03 missing test pin for as.go yasm DepRefs; D04 defaultProgramPeerdirsFor inner self-suppression dead code; D05 executor edited tasks.md (process boundary; orchestrator-owned). **D01-D04 fixed in round 2. D05 resolved at squash time (orchestrator overwrites).** Round 2 (1 nit): D06 gen.go:747 comment misnames `genModule` as `defaultPeerdirsFor`. **Deferred — 1-token edit, picks up in next gen.go cleanup PR.**
  Surprises / constraints future PRs must respect:
  - **NodeRef zero-value collision**: `NodeRef{} == NodeRef{id:0}` is the same as the FIRST emitted node (BufferedEmitter starts ids at 0). Any "is this NodeRef set?" check via zero-value comparison is structurally broken. PR-30 D04 introduced `HasGenerator bool` to ModuleCCInputs as the workaround; M3+ may want a sentinel-id (e.g. -1 for unset) or a separate Optional[NodeRef] type.
  - **`asmlib` uses `.asm` extension, not `.S`**. EmitAS now dispatches both. PR-31+ rule emitters introducing new asm sources MUST handle `.asm`.
  - **Reference invariant: every AR has zero AR-on-AR deps**. PR-30 D05 dropped peer-AR refs from LIBRARY's AR DepRefs. Peer-AR refs are STILL needed by LD (PROGRAM linker stage); only AR's own DepRefs lose them. Future emitters MUST keep this distinction: AR.DepRefs = own .o files only; LD.DepRefs = own + peer-AR + peer-Global archives.
  - **SRCDIR semantics: PROGRAM rebases module_dir; LIBRARY keeps module_dir + uses `__/<rel>` infix**. PR-28-D02's unconditional rebase was wrong for sibling-SRCDIR (libcxxabi-parts, musl_extra) AND ancestor-SRCDIR + LIBRARY (tcmalloc/no_percpu_cache). PR-30-D06 fixes all 4 in-closure shapes. The stat-based local-vs-SRCDIR check makes gen output dependent on filesystem state — synthetic tests bypass via empty SourceRoot. M3+ streaming emitter MUST preserve this stat dependency or replace with explicit metadata.
  - **`hadAllocator` and `muslLite` per-module flags** are computed in collectStmts and consumed in defaultProgramPeerdirsFor at gen.go:932. They are NOT part of the ModuleInstance memo key (D34) — works today because one ya.make per path. M3+ may need to widen the key if PROGRAM modules become multi-instantiable.
  - **`defaultProgramPeerdirsFor`** is the M2-equivalent of `defaultPeerdirsFor` for PROGRAM modules. Adds musl/full (when not MUSL_LITE) + ALLOCATOR-impl-of-choice (when no explicit ALLOCATOR). Suppression via `instance.Target == cfg.Target.ID` host-axis check (PR-28-D08 lesson) — do NOT use Flags.PIC.
  - **Yasm cycle prevention via ENABLE(MUSL_LITE)**: yasm's own ya.make declares ENABLE(MUSL_LITE), which sets `d.muslLite = true` and prevents defaultProgramPeerdirsFor from adding musl/full. Without this, yasm → musl/full → asmlib (via ARCH_X86_64) → yasm cycle inflates count from 7 to 32. The fix is per-module not global; ENABLE/DISABLE routing through applyUnknownStmt is the project standard for boolean macros.
  - **L0 cost cascade for Generator → DepRefs is recoverable in PR-30 context**: PR-29 D07 deferred Generator wiring fearing 2 lost L0 multiset matches. PR-30's +400-node closure widening absorbed the cost; final L0 is +10.43pp net gain. Future deferred-cost decisions should re-evaluate after major coverage expansions.
  - **Stat-based composeCCPaths is filesystem-coupled**: cc.go's local-vs-SRCDIR resolver uses os.Stat(filepath.Join(sourceRoot, instance.Path, srcRel)) to decide whether to rebase. Synthetic tests pass empty SourceRoot to bypass. Production gen runs against real source tree. M3+ streaming variants MUST decide: keep stat dependency (simple but fragile under reorganized trees) OR pre-compute "source files in this module" metadata.
  - **47-12 = 35 net unpaired pairs remaining**: 12 reference module_dirs entirely accounted for. The 35 unpaired-want (reference nodes we don't emit) + 12 net unpaired-got (we emit nodes reference doesn't) are PR-31 polish territory. Likely sources: small ALLOCATOR variants, SRCDIR edge cases, asmlib L3 cmd_args (clang invocation correct topologically but cmd_args differ from reference's yasm-rendered shape).
  - **L3=80.35% / 660 remaining divergent pairs**: PR-31 closes via per-host-bundle CC tuning (libcxx host, libcxxrt host, jemalloc/mimalloc CC bundles) plus AS bytewise tuning (asmlib's 25 nodes still emit clang invocation; reference uses yasm — needs hand-translated yasm composer). PR-31 is M2 polish, NOT required for acceptance.

- **PR-31** (2026-05-07) — **Include scanner + sg.json reference switch + per-path GLOBAL ADDINCL routing (M2 L2 recovery).** Plan: `docs/drafts/20260507-1544-pr31-include-scanner.md`. Triggered mid-session when user clarified canonical reference is `sg.json` (NOT g.json); sg.json includes parsed `#include` paths as node `inputs`. Switching the comparator alone crashed L2 from 97.56% (vs g.json) to 0.72% (vs sg.json) because every CC node was missing its include closure. **Headline: L2 0.72% → 79.92% (M2 acceptance gate ≥70% cleared). All 4 M2 gates now met against sg.json: L0=98.77%, L1=98.74%, L2=79.92%, L3=80.35%.** M1 byte-exact preserved (2/2). Cycles: 7 (unchanged).
  Substantive work:
  - **D01** sg.json const switch in gjson.go + propagation to 11 test files
  - **D02** `sysincl.go` (~510 LOC) — minimal hand-rolled YAML loader for `<sourceRoot>/build/sysincl/*.yml` (22 files loaded out of 53 per build/conf/sysincl.conf's M2 conditional; 173 records, 5370 mappings, ~3ms load time, dual-platform aarch64+x86_64 instantiation). Documented exception to project's "hand-translate build/conf" rule per user-confirmed decision. Fail-soft on unsupported source_filter patterns (esp-idf, python, unsorted, windows) with stderr diagnostic
  - **D03** `scanner.go` (~510 LOC) — `IncludeScanner` with regex matcher `^\s*#\s*(include|include_next)\s*[<"]([^>"]+)[>"]`; transitive walk + cycle detection + per-file caching; resolution algorithm: same-dir for quoted, then per-module ADDINCL (own + GLOBAL), then sysincl mappings; fan-out (single header → multiple paths from sysincl); empty-prefix BasePath (round-2 D01 fix) for `$(SOURCE_ROOT)`-rooted system-form includes
  - **D04-D06** Peer-propagated GLOBAL ADDINCL routing: `applyAddInclStmt` strips GLOBAL via per-path `splitAddInclPaths` (round-3 D13 fix replaced statement-level helper); `moduleEmitResult.AddInclGlobal` + walker aggregation `walkPeersForGlobalAddIncl`; `ModuleCCInputs.PeerAddInclGlobal` + cmd_args slot. Musl flavours drop both own-AddIncl and PeerAddInclGlobal (`-nostdinc` + muslCcIncludes baked in)
  - **D07** dual-platform `genCtx.scannerTarget` (aarch64) + `genCtx.scannerHost` (x86_64) — needed because linux-musl-aarch64.yml vs linux-musl.yml mutually exclusive in upstream
  - **D08-D09** EmitCC appends `IncludeInputs` to Node.Inputs after primary source path
  - **D10-D11** AR/LD/AS member-input aggregation (deviation from plan): sg.json's AR/LD/AS pull each member CC's source + transitive headers into their own `inputs`. M1 byte-exact requires this. `EmitAR`/`EmitARGlobal`/`EmitLD` gained `memberInputs []string`; `EmitAS` gained `includeInputs []string`; `emitOneSource` returns 4-tuple
  - **D12** L2 multiset comparator (deviation from plan): `compare_props.go::l2Match` flipped from order-sensitive to multiset (new `stringMultisetEqual` helper). Without flip, L2 would stay ~1.7% even with correct scanner
  - **D13 (round-3 fix)** Per-path GLOBAL ADDINCL semantics: `AddInclStmt.{Modifier, Paths}` replaced with `{GlobalPaths, OwnPaths}`; new `splitAddInclPaths` walks args left-to-right, classifies path immediately following `GLOBAL` as global, others as own. Eliminated spurious `-I libcxx/src` leak on ~110 non-musl CC consumers of libcxx
  Files: 3 new (`sysincl.go`, `scanner.go`, `sysincl_test.go`); 18 modified (rule emitters, gen.go, comparator, tests, gjson const). +2407 / -155 LOC.
  Three review rounds. Round 1 (11 defects, 2 major): D01 scanner missing `$(SOURCE_ROOT)` BaseSearchPath; D03 vacuous JS/LD prefix tests; plus 9 nits/minors. Round 2 (2 new): D12 musl-arch -I gap (deferred to PR-32 — implementing here would bake more musl-hardcoding before the dedicated refactor); D13 libcxx/src ADDINCL leak. Round 3 (D13 fix): per-path GLOBAL semantics. **Round 3 review: NO NEW DEFECTS.** D12, D08, D09, D11 all deferred (D12 to PR-32, D08/D09 to PR-32+ as JS/LD scope expansion, D11 auto-resolves with D12).
  Surprises / constraints future PRs must respect:
  - **Reference graph is `sg.json`, NOT `g.json`** going forward. All UIDs differ between the two graphs (Merkle hashes include inputs; sg.json adds the include closure). Don't accidentally reintroduce g.json references.
  - **`sysincl.go` is the runtime-parsed exception**: 11K-line YAML data tables loaded at startup, NOT hand-translated. Documented in memory `sysincl_yaml_runtime_parse.md`. Don't auto-extend this exemption to other directories.
  - **Per-path GLOBAL ADDINCL semantics**: the PR-29-D01 helper `splitGlobalModifier` (statement-level — for CXXFLAGS/CONLYFLAGS) and the new `splitAddInclPaths` (per-path — for ADDINCL) are NOT interchangeable. ADDINCL has per-path GLOBAL keywords like `ADDINCL(GLOBAL a b GLOBAL c d)`; CXXFLAGS uses statement-level prefix. If PR-32 routes peer-GLOBAL CFLAGS, it likely wants per-path semantics too — promote to general helper.
  - **Dual-platform scanners required** because aarch64-vs-x86_64 sysincl mappings differ (esp. musl bits/* arch-specific headers). `genCtx.scannerTarget` + `genCtx.scannerHost` constructed eagerly in `Gen`. M3 streaming variant must replicate.
  - **L2 multiset comparison**: `compare_props.go::stringMultisetEqual` for inputs (binary, no partial credit); tags remain order-sensitive. M3+ comparator changes must respect this asymmetry.
  - **AR/LD/AS member-input aggregation**: `EmitAR`/`EmitLD` take `memberInputs`; `EmitAS` takes `includeInputs`; `emitOneSource` returns 4-tuple (the per-CC input slice walker accumulates). New rule emitters (PR-32+) must follow this signature pattern.
  - **L2 stagnates at 79.92%** because PR-31-D12 (musl-arch -I gap from upstream's `when ($MUSL == "yes") { PEERDIR+=contrib/libs/musl/include }` auto-PEERDIR + ARCH-IF evaluator gap) is deferred to PR-32. PR-32's musl refactor will close it via flag-driven peer-include mechanism instead of new musl hardcoding. Estimated lift: ~22pp L2 + L3 in lockstep.
  - **JS / LD emitters under-emit inputs (D08/D09)**: JS-derived CC nodes' inputs don't include per-source include closure (~941 entries missing per JS CC; 23 such nodes); LD doesn't include peer-archive paths or per-member-CC closures. Tests hard-pinned to current K (8 for JS, 1020 for LD) so future regressions trip; full-set expansion deferred to PR-32+.
  - **Stderr noise on sysincl unsupported patterns**: 7 records skipped per scanner instantiation (windows.yml `case_sensitive: false`, unsorted.yml negative-lookahead patterns), printed twice (target + host scanner). Cosmetic but verbose; could move to one-shot summary or a verbosity flag.
  - **target/host counts unchanged from PR-30 (1916/1802)** — earlier baselines (PR-29's 1739/1582) were against g.json; sg.json/PR-30/PR-31 baseline is 1916/1802.

- **PR-32** (2026-05-07) — **musl refactor (flag-driven) + auto-PEERDIR `contrib/libs/musl/include` + peer-propagated GLOBAL CFLAGS/CXXFLAGS/CONLYFLAGS routing.** Plan: `docs/drafts/20260507-2137-pr32-musl-refactor.md`. **L2 79.92% → 83.27% (+3.35pp); L3 80.35% → 80.51% (+0.16pp).** L0=98.77%, L1=98.74% (preserved). M1 byte-exact preserved (2/2). Cycles: 7 (unchanged). Target=1985, host=1733, total=3718.
  Substantive work (12 D-tasks):
  - **D01** `--define KEY=VALUE` CLI flag (`stringMapValue` flag.Value impl; rejects bare KEY) + `cliDefines map[string]string` plumbed through `Gen` → `GenWith(cfg, root, target, defines)` → `genCtx.cliDefines`. M2 default `{"MUSL": "yes"}` preserves PR-31 behavior when flag unspecified.
  - **D02** `Flags.LibcMusl` field + dispatch flip in `cc.go::EmitCC` (was path-prefix `HasPrefix("contrib/libs/musl")`) + `gen.go::includeScannerBasePaths`. Single backward-compat shim retained at `module.go::inferFlagsFromPath` seeding LibcMusl from path (M5+ replaces with macro-driven inference parsing NO_PLATFORM + SET(MUSL no) from ya.make).
  - **D03** auto-PEERDIR `contrib/libs/musl/include` for non-LibcMusl modules (mirrors upstream `_BASE_UNIT::when ($MUSL == "yes") { PEERDIR+=musl/include }`). Deviation: also fires for runtime-ancestor modules (libcxx, util) per upstream's `_BASE_UNIT` derivation semantics. PR-32 round-2 D02 fix added `cliMuslOn(ctx)` to the `contrib/libs/musl` source-bearing peer gate too — `--define MUSL=no` now produces coherent musl-free graph (665 nodes vs MUSL=yes 3718 nodes).
  - **D04-D06** Typed `*CFlagsStmt` / `*CXXFlagsStmt` / `*CONLYFlagsStmt` parsers replacing UnknownStmt fallback; per-path `splitFlagsByGlobal` (mirrors PR-31-D13's per-path `splitAddInclPaths` shape). Tests: `TestParseCFlags_PerPathGlobal`, `TestParseCXXFlags_Global`, `TestParseCONLYFlags_Own`.
  - **D07** Walker two-phase peer-GLOBAL aggregation: own across all peers FIRST, then transitive across all peers (matches reference cmd_args ordering). 4-axis aggregation (ADDINCL + CFLAGS + CXXFLAGS + CONLYFLAGS).
  - **D08** ModuleCCInputs gains 4 PeerXxxGlobal axes; cmd_args composer splits into TWO slots: `autoPeerCFlags` between catboost and 2nd noLibcUndebugBlock; `peerExtras` after own CXXFLAGS, before builtinMacroDateTime. Plan called for one combined slot; reference shape required two.
  - **D09** Auto-injected `-D_musl_` peer-CFLAG via `defaultPeerCFlags` (consumer-side; `-D_musl_=1` is musl-self-only, lives in muslExtraDefines).
  - **D10** `ld.go::composeLDCmdVcsCompile` flag-driven `-D_musl_=1`/`-D_musl_` injection only when `muslOn`; EmitLD takes `muslOn` parameter; M2 callers pass true.
  - **D11** Defects.md PR-31-D12 root-cause amendment (orchestrator-handled).
  - **D12** cc.go header docstring refresh.
  Files: 9 modified (cc.go +140, gen.go +585 [the bulk], yamake.go +74, main.go +61, ld.go +33, module.go +16, gen_test.go +42, ld_test.go +5, yamake_test.go +78); 1 added (.gitignore). +822/-218 LOC.
  Two review rounds. Round 1 (3 defects, 0 major): D01 "single outlier `-D_musl_=1`" claim understated scope (89 missed instances actually — yasm 79 + ragel6 9 + archiver 1; orchestrator-amended in defects.md); D02 `--define MUSL=no` internally inconsistent; D03 `_artifacts/` not in `.gitignore`. **D02-D03 fixed in round 2.** Round 2: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **L2 ceiling at 83.27% — util-subtree libcxx/libcxxrt peering blocked by `runtimeAncestorPaths`.** Resolution requires re-examining the runtime-ancestor exclusion model (which modules get which auto-peers). Deferred to **PR-33** alongside the libcxx peer-CFLAGS bundle (CATBOOST_OPENSOURCE second copy, -DLIBCXXRT, libcxx warning-extension bundle). PR-33 estimated lift: L2 → 90%+, L3 → 90%+.
  - **musl-mention count went UP (231 → ~335)** despite the user directive. Path-prefix dispatch IS eliminated (the architectural goal); remaining mentions are honest flag-driven identifiers (`Flags.LibcMusl`, `cliMuslOn`, `composeMusl*`, `muslConsumerSentinel`) + genuinely musl-specific data bundles (muslCcIncludes, muslExtraDefines). User confirmed acceptable.
  - **`cliDefines map[string]string` is the new CLI surface for build configuration.** M2 keys: `MUSL`. Future keys: `OPENSOURCE`, `BUILD_TYPE`, etc. Default M2 binding `{"MUSL": "yes"}` preserves PR-31 behavior when `--define` unspecified. Tests that bypass `Gen` and call `defaultPeerdirsFor` directly should pass nil ctx (cliMuslOn returns true for nil — M2 backward-compat).
  - **Two-slot cmd_args composer**: `autoPeerCFlags` slot = consumer-side `-D_musl_` + peer-GLOBAL CFLAGS; `peerExtras` slot = peer-GLOBAL CXXFLAGS + peer-GLOBAL CONLYFLAGS (positioned after own CXXFLAGS). Future PRs adding more peer-propagated flag axes MUST decide which slot they belong in based on reference shape, NOT a single combined slot.
  - **musl-self GLOBAL CFLAGS suppressed from peer-propagation** to prevent 460-node over-propagation of `-D_musl_=1`. The 89 remaining `-D_musl_=1` mismatches in yasm/ragel6/archiver originate from `IF (MUSL) CFLAGS(-D_musl_=1)` own-CFLAGS branches not yet evaluated true for those host modules — separate L2/L3 ceiling work, not PR-32 scope.
  - **Per-path GLOBAL semantics for CFlags/CXXFlags/CONLYFlags** mirrors PR-31-D13's ADDINCL fix. PR-29-D01's older `splitGlobalModifier` (statement-level) only applies to UnknownStmt fallback paths — typed Stmts use `splitFlagsByGlobal`. Future macro-typed Stmts that take GLOBAL must use the per-path helper.

- **PR-33** (2026-05-07/08) — **util-libcxx peering + own-GLOBAL flag routing + cxx warnings + util PEERDIR ordering + yasm `-D_musl_=1`.** Plan: `docs/drafts/20260507-2340-pr33-libcxx-peering.md`. **Headline: L3 80.51% → 95.60% (+15.09pp).** L2 83.27% → 83.94% (+0.67pp; sysincl ceiling deferred to PR-35); L0=98.77%, L1=98.74% (preserved); M1 byte-exact preserved (2/2); cycles=7; target=1916, host=1802.
  Substantive work:
  - **D01** isRuntimeAncestor literal-only (drop subtree extension); unblocks util-subtree libcxx/libcxxrt peering. Cycles unchanged at 7. Test `TestIsRuntimeAncestor_LiteralOnly` added.
  - **D02** Own GLOBAL ADDINCL/CFLAGS/CXXFLAGS/CONLYFLAGS routing into ModuleCCInputs + cmd_args via `composeOwnAndPeerGlobalBucket` helper (own GLOBAL ∪ peer GLOBAL deduped, emitted twice flanking catboost-redux for C++ sources). ADDINCL merges GlobalPaths+OwnPaths into d.addIncl per-stmt declaration order.
  - **D03** Own non-GLOBAL CFLAGS slot between commonDefines and 1st noLibcUndebugBlock; closes -DLIBCXXRT (116 libcxx CC), -DTCMALLOC_* (71 nodes), -funroll-loops (77 jemalloc), -Wnarrowing (20 util).
  - **D04** cxxStandardWarnings 10-arg bundle (matches ymake_conf.py:1624-1636); slotted between -std=c++20 and own non-GLOBAL CXXFLAGS.
  - **D05** sysincl tightening DEFERRED — planner recipe ineffective (per-record source-vs-includer routing required, beyond PR-33 mechanical scope).
  - **C01 (round-1 fix)** util PEERDIR ordering: conditional hoist of runtime-stack ADDINCL paths (libcxx/include, libcxxrt/include, libcxxabi/include, libunwind/include) to FRONT of peerAddInclGlobal, gated on `isRuntimeAncestor(consumer.path)`. yasm host (NO_RUNTIME) keeps libcxx/libcxxrt at TAIL. 19 util CC nodes lift L3.
  - **C02 (round-1 fix)** yasm `-D_musl_=1` injection: PROGRAM-only append to ownCFlags, gated `MUSL=yes && !LibcMusl && !effectiveNoPlatform`. Discriminator is PROGRAM-vs-LIBRARY (structural), not host-vs-target (axis): every PROGRAM CC carries `-D_musl_=1`; no LIBRARY CC does. 79 yasm CC nodes lift L3.
  - **D06** M1 byte-exact preserved.
  - **D07** L0/L1 preserved; L3 target met (95.60% > 89%); L2 below planner target (83.94% < 88%).
  - **D08** PR-31-D11 (AR member-input) auto-resolved upstream by D02-D04.
  Files: 5 modified (cc.go +213, gen.go +234, gen_test.go +109, flags.go +28, scanner.go +17). +504/-97 LOC.
  Three review rounds. Round 1 (15 defects across 3 parallel reviewers): A clean (correctness); B 8 architectural minors/nits deferred to M5 hardening (B01-B08); C 5 empirical findings (C01 util PEERDIR ordering MAJOR, C02 yasm -D_musl_=1 MAJOR, C03 sysincl uchar.h fan-out MAJOR deferred to PR-35, C04 libcxxrt count correction, C05 F5 mislabel correction). **C01+C02 fixed in round 2** (+98 nodes lift). Round 2 (15 more defects across 2 parallel reviewers): A2 8 discriminator-correctness findings (A2_01 malloc/api hoist injection MAJOR, A2_02 AS x86_64 toolchain MAJOR — see below; rest minor/nit deferred); C2 7 empirical findings (C2_01 same as A2_02; C2_02-C2_07 corrections + pre-existing/separate-PR deferrals). **A2_01, A2_02, A2_06 deferred to PR-35 because user-imposed HARD perf gate (PR-34) takes priority — fixing them in PR-33 would extend scope while perf is broken.**
  Surprises / constraints future PRs must respect:
  - **`isRuntimeAncestor` is now literal-only** (no subtree HasPrefix extension). Per-path self-cycle guards in defaultPeerdirsFor are now structurally needed (defense-in-depth). Previously the subtree extension covered them; now they're load-bearing.
  - **C01 hoist is REORDER-only, not INJECT** (PR-33-A2_01 deferred). For runtime ancestors with sparse PEERDIRs (library/cpp/malloc/api), libcxx/libcxxrt are missing entirely from cmd_args. PR-35 fix: augment defaultPeerdirsFor to add libcxx/include + libcxxrt/include as implicit GLOBAL header-only contributions for runtime ancestors.
  - **AS-emitter hardcoded aarch64 toolchain** (PR-33-A2_02/C2_01 deferred). 62 x86_64 AS nodes have wrong cmd_args (aarch64-linux-gnu, -march=armv8-a). Plus 41 musl-self assembly nodes lack `-D_musl_=1`/muslExtraDefines. **Dominant remaining L3 lever — closing PR-35 will lift L3 95.60% → ~97.10%.** Pattern in cc.go composeMuslHostCC; needs replication in EmitAS via target-vs-host dispatch.
  - **C02 PROGRAM-vs-LIBRARY discriminator empirically holds** for non-musl-self CC nodes in M2 closure (zero over-emit, zero under-emit). Future PROGRAMs with NO_PLATFORM won't get `-D_musl_=1` per the gate; M3+ should re-verify against reference.
  - **`composeOwnAndPeerGlobalBucket` emits twice flanking catboost-redux for C++ sources** — empirically driven by reference shape (every C++ CC has catboost=2; every C CC has catboost=1). C path uses peerExtras slot only.
  - **8 architectural cleanups (B01-B08) deferred to M5 hardening** — ADDINCL/CFLAGS data-shape asymmetry, composer 11-positional-arg regression of PR-29 struct refactor, "bucket" terminology, etc. Functional behavior correct; future structural work.
  - **L3 ceiling at 95.60% has 117 remaining divergent pairs**: top clusters are AS x86_64 toolchain (56 nodes — PR-35), libcxxrt missing -nostdinc++ (14 nodes — separate fix), libunwind/libcxxabi-parts -nostdinc++ shortfalls (16 nodes), ragel6 host PIC duplicate linux-headers (9 nodes — PR-35), util/system/compiler.cpp.o unpaired (1+1 AR — separate parser gap), assorted minors.
  - **L2 ceiling at 83.94% with 552 divergent pairs**: dominantly sysincl uchar.h fan-out (251 libcxx/abseil consumers — PR-35), other sysincl per-record routing edges, JS/LD scope expansion (PR-31-D08/D09).
  - **HARD perf constraint added 2026-05-08**: `./yatool gen --target tools/archiver` ≤5s. Currently 28s. PR-34 emergency. Future PRs must verify perf in their acceptance bar.

- **PR-11** (2026-05-07) — STYLE.md retrofit of PR-01/02/07 output. Two tracks: (1) error handling — `Finalize(*BufferedEmitter) *Graph` (was `(*Graph, error)`) raises internal errors via `ThrowFmt`; `canonicalNodeBytes(*Node) []byte` (was `([]byte, error)`) raises via `Throw`; `Parse`/`ParseFile` keep `(*MakeFile, error)` signature (typed `*ParseError` is a domain signal for future CLI display) but internal helpers throw, with the public functions wrapping in `Try` and converting back via `exc.AsError()` (which returns the underlying `*ParseError` directly so callers' `errors.As` works). `main()` wraps `dispatch(argv)` in `Try(...).Catch(...)` per STYLE.md "Catches at boundaries". (2) formatting — blank-line rules around control blocks and before `return`, logical grouping. Files touched: `main.go`, `emitter.go`, `uid.go`, `yamake.go`, `emitter_test.go`, `uid_test.go`. Untouched (no error-return retrofit needs): `node.go`, `node_test.go`, `yamake_test.go`. Tests rewritten where blocking: error-expecting tests use new `finalizeExc` helper that runs `Finalize` inside `Try` and returns `(*Graph, *Exception)`; success-path tests call `Finalize` directly so unexpected panics surface as test failures. Verification: `go build/vet` clean; `gofmt -l` empty; 37 top-level tests PASS.
  Two review rounds. Round 1 (12 defects, 0 major): 8 minor blank-line misses inside loop bodies (D01–D08, mostly pre-existing PR-02/07 patterns the retrofit was supposed to clean); 4 nits — D09 missing blanks after `for {...}` in tests (verified: round-1 reviewer was wrong, blanks were already present), D10 dead `errors.As` reassignment in Parse/ParseFile (no-op against today's throw paths), D11 dispatch comment over-promised `os.Exit` invariance, D12 `finalizeExc` docstring missing warning. **All 12 fixed** in round-2 commit; D10 deleted dead code AND dropped now-unused `errors` import from yamake.go. Round 2: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **`Finalize` no longer returns `error`** — all callers (PR-10 `gen` driver, future M-line tests) must wrap in `Try(func(){ g = Finalize(e) })` and check `*Exception`. The error-expecting test pattern is captured in `finalizeExc` helper at `emitter_test.go:62-72`; reuse rather than re-invent.
  - **`canonicalNodeBytes` no longer returns `error`** — all callers similarly. Internal use of `Throw` means `json.Encoder.Encode` failure becomes a panic, not a returned error. (`json.Encoder.Encode` failure is essentially impossible for our types — but the panic is the documented contract now.)
  - **`Parse`/`ParseFile` keep `(*MakeFile, error)` signature** — the typed `*ParseError` flows through directly via `exc.AsError()`. Future PR-10 (`gen` driver) and PR-08 (`cc` rule) consumers of parsed `ya.make` MUST `errors.As(err, &pe)` to extract `pe.File`/`pe.Line`/`pe.Col` for human-readable error messages.
  - **`dispatch(argv)` extracted from `main`** — calls `os.Exit(cmdGen(...))` after `cmdGen` returns. Panics during `cmdGen` propagate to outer `Try.Catch`. CLEAN exits (`os.Exit(0)`) bypass any deferred cleanup placed by Try. If a future PR adds defer-based cleanup (profile flush, log close), restructure dispatch to RETURN an exit code, not call `os.Exit` directly. Comment in `main.go` documents this.
  - **D17/D18 enforcement going forward** — every executor brief from PR-03 onward MUST include "code follows STYLE.md" + a quick checklist (no `if err != nil { return err }`; blank lines around control blocks; main/goroutine entries wrapped in Try). Every reviewer brief MUST audit STYLE.md compliance as a first-class concern.
  - **`finalizeExc` is for error-expectation tests ONLY.** Success-path tests must call `Finalize(e)` directly so unexpected panics fail loudly. Docstring captures this contract; reviewers should flag any wrap-the-success-path use.
  - **Test file convention now mixed:** `emitter_test.go` uses `finalizeExc` + throw-style; `node_test.go`/`yamake_test.go` keep classic `if err != nil { t.Fatalf(...) }` (allowed under STYLE.md "Discriminate" — tests branch on the error to fail with a diagnostic). Both are acceptable. Reviewers should NOT push to standardize one way; pick whichever reads cleaner per file.

- **PR-03** (2026-05-07) — On-disk g.json reader + `inspect` subcommand wiring. `LoadReference(path string) *Graph` slurps the file via `os.ReadFile` (~27 MB; D4 stays with stdlib `encoding/json`, no streaming machinery), `json.Unmarshal` into the existing `Graph` from `emitter.go`, validates non-empty `Graph` AND `Result`, throws on any error. Decoded `*Node`s have JSON-tagged fields populated and internal `*Refs` nil (correct: reference is already-finalized, UIDs are the truth). `cmdInspect` rewired: `flag.NewFlagSet(..., ContinueOnError)` + `fs.SetOutput(io.Discard)` (suppress flag's auto-output so throw path owns diagnostics), discriminates `flag.ErrHelp` for clean exit-0 + usage-on-stdout, parses 1 positional arg, calls `LoadReference`, prints `nodes: N  results: M  platforms: P1,P2,...` (platforms sorted via slice + `sort.Strings`, per D14). New `printInspectUsage(w io.Writer)` helper. Files: `gjson.go`, `gjson_test.go`, modified `main.go`. Verification: `go build/vet` clean; `gofmt -l` empty; 8 new tests PASS (6 LoadReference + 2 cmdInspect); smoke confirms `inspect` against real g.json prints `nodes: 3730 results: 1 platforms: default-linux-aarch64,default-linux-x86_64`, exit 0; error smokes (`-bogus`, `/nonexistent`, no-arg) all exit 1 with EXACTLY ONE error line on stderr (not duplicated).
  Two review rounds. Round 1 (2 minor):
  - **D01 (minor)** — `inspect -h` exited 1 with cryptic "flag: help requested" (sentinel not discriminated). **Fixed** by `errors.Is(err, flag.ErrHelp)` branch printing usage to stdout + return 0.
  - **D02 (minor)** — `inspect -bogus` printed unknown-flag error twice (flag's auto-output AND throw path). **Fixed** by `fs.SetOutput(io.Discard)`.
  Round 2 (1 nit, deferred):
  - **D03 (nit, deferred)** — `TestCmdInspect_UnknownFlag_PanicsWithSingleErrorMessage` test name overstates: it can only assert exception payload contains substring, not "exactly one occurrence on stderr". The dedup guarantee is verified manually via `./yatool inspect -bogus 2>&1`. Defer: rename or shell-out test in future test-infra PR.
  Surprises / constraints future PRs must respect:
  - **`flag.ContinueOnError` + `SetOutput(io.Discard)` + `errors.Is(err, flag.ErrHelp)` + `printXxxUsage(w io.Writer)` pattern is now the project standard for subcommands.** PR-10's `cmdGen`/`cmdCompare` MUST use the same shape (also retires PR-01-D05 deferred ceremony — `_ = flag.NewFlagSet(...)` lines and `"flag"` import still present in stubs for `cmdGen`/`cmdCompare` until they get real implementations).
  - **`Throw(err)` is nil-safe** (per `throw.go:48`), so the `if errors.Is(err, flag.ErrHelp) { return 0 }` followed by unconditional `Throw(err)` is correct even on the success path (`err == nil`).
  - **Test that pins single-error-line behaviour is currently MANUAL.** D03 deferred; if a future PR introduces stderr capture infrastructure, fold a regression test for D02 into it.
  - **`LoadReference` returns `*Graph` (not `(*Graph, error)`)** per D17. Callers must wrap in `Try(...)` if they want to discriminate IO/parse errors. Current caller (cmdInspect) lets the throw propagate to main's outer Catch.
  - **Decoded reference Nodes have nil `DepRefs`/`ForeignDepRefs`** — pinned by `TestLoadReference_Synthetic`. Don't try to use them as "in-progress" emitter inputs; they're terminal data.

- **PR-08** (2026-05-07) — Hardcoded CC rule for the M1 leaf module `build/cow/on/lib.c`. Three new files: `toolchain.go` (`PlatformConfig` + `TargetCfg{Name: "default-linux-aarch64"}` — placeholder for M5 expansion); `flags.go` (named `[]string` bundles totaling 93 args + composition prologue: `ccIncludes`/`debugPrefixMapFlags`/`xclangDebugCompilationDir`/`commonCFlags`/`warningFlags`/`commonDefines`/`noLibcUndebugBlock` (used twice)/`catboostOpenSourceDefine`/`builtinMacroDateTime`/`macroPrefixMapFlags`); `cc.go` (`EmitCC(cfg, moduleDir, srcRel, emit) NodeRef` composes 7-arg prologue + bundles + 1 trailing input = 101 args, byte-exact match against reference). Test `TestEmitCC_BuildCowOnLibC_ByteExact` loads g.json, locates ref node by output path, asserts every field byte-exactly (cmd_args entry-by-entry, env, inputs, outputs, kv, tags, target_properties, requirements, top-level env, platform, host_platform=false, foreign_deps=nil). Verification: `go build/vet/gofmt` clean; 1 new test PASS (38 total).
  Two review rounds. Round 1 (3 nits): D01 stray blank-after-`{` in 3 spots (cc_test.go); D02 env map literal aliased between Cmds[0].Env and node.Env (latent footgun, deferred with explanatory comment); D03 no-libc bundle unconditional (TODO already in code, deferred). **D01 fixed**, D02/D03 deferred-with-rationale. Round 2 (1 nit, deferred): D04 D02 comment wraps at >78 chars (cosmetic; reflow when next editing cc.go).
  Surprises / constraints future PRs must respect:
  - **Reference CC node `target_properties` has ONLY `module_dir`** (no module_lang/type/tag) — pinned in `EmitCC`. Other CC nodes in g.json may have more keys; future generic CC rule must pin per-node, not assume a uniform shape.
  - **`Requirements` numeric values stored as `float64`** (`float64(1)`, `float64(32)`) so `reflect.DeepEqual` against JSON-decoded reference holds. Go's `encoding/json` decodes JSON numbers into `float64` for `interface{}` targets — using `int` would silently fail comparison.
  - **`noLibcUndebugBlock` referenced TWICE** in EmitCC composition with `-DCATBOOST_OPENSOURCE=yes` between — this is the actual reference shape, NOT a duplication mistake. Verified by per-arg enumeration of 101 args.
  - **`Cmds[0].Env` and top-level `Env` are aliased** to the same `map[string]string` literal. Safe for single-shot EmitCC; future PRs that mutate emitted nodes post-emit MUST clone both before mutating either. Comment in cc.go documents this constraint.
  - **No-libc bundle is currently unconditional** (cc.go TODO). The next CC leaf without `NO_UTIL`/`NO_LIBC`/`NO_RUNTIME` will need a module-flavor flag passed into EmitCC to gate `noLibcUndebugBlock` and `catboostOpenSourceDefine`. Logged as PR-08-D03 deferred.
  - **`EmitCC` is byte-exact for `build/cow/on/lib.c` ONLY.** Other inputs produce structurally-correct nodes but with wrong flags. M2 will generalize.

- **PR-09** (2026-05-07) — Hardcoded AR rule for the M1 leaf module `build/cow/on`. `ar.go` ships `EmitAR(platform, moduleDir, objRefs []NodeRef, objPaths []string, emit Emitter) NodeRef` — composes 11-arg cmd_args (`[/ix/realm/pg/bin/python3, $(SOURCE_ROOT)/build/scripts/link_lib.py, ar, GNU_AR, None, $(BUILD_ROOT), None, --, --, archivePath, objPath]`), pins env (cmd-level + top-level), inputs (`.o` + `link_lib.py` script), outputs (`$(BUILD_ROOT)/<moduleDir>/lib<dashed>.a`), kv (`p:AR`), tags (empty), target_properties (`module_dir`/`module_lang`/`module_type` — NO `module_tag` for THIS node), platform, requirements, host_platform=false, foreign_deps=nil. Length validation `if len(objRefs) != len(objPaths) { ThrowFmt(...) }` is the first stmt. Two tests: `TestEmitAR_BuildCowOn_ByteExact` (byte-exact) + `TestEmitAR_LengthMismatchPanics` (precondition). Verification: `go build/vet/gofmt` clean; 2 new tests PASS (39 total — combined with PR-08).
  Two review rounds. Round 1 (6 defects, 0 major): D01 docstring overstated naming generality; D02 multi-source order not sorted (deferred to M2); D03 no length validation; D04 hardcoded python path no TODO; D05 test missing DepRefs len check; D06 env duplication intent undocumented (deferred). **D01/D03/D04/D05 fixed**, D02/D06 deferred. Round 2: NO NEW DEFECTS.
  Surprises / constraints future PRs must respect:
  - **Archive-naming convention is M1-LEAF-ONLY.** `"build/cow/on" → "libbuild-cow-on.a"` via `"lib" + ReplaceAll(moduleDir, "/", "-") + ".a"` works for `build/cow/on` only. Real ymake applies prefix-truncation rules: `contrib/...` drops leading `contrib`; `library/...` drops leading `library`; `util` becomes `libyutil.a` (special `y` prefix). M2 multi-leaf rule emitter MUST implement the real naming function. TODO in ar.go documents this.
  - **Multi-source AR is NOT correct yet.** EmitAR appends `objPaths` to cmd_args and inputs in caller order; real ymake sorts alphabetically. M1 leaf has 1 source so this doesn't matter; M2 multi-source will need sort. PR-09-D02 deferred logs the constraint.
  - **`/ix/realm/pg/bin/python3` is hardcoded** from the reference build environment. Any other host produces byte-mismatch. Future portability work must template from `TargetCfg` or detect from `$PATH`. TODO in ar.go documents.
  - **`EmitAR` panics on `len(objRefs) != len(objPaths)`.** PR-10's `gen` driver wiring CC outputs to AR depends on lockstep parallel slices — a slip (e.g. adding a path without matching ref) is now a hard error, not a silent malformed Node.
  - **`cmdEnv` and `topEnv` are SEPARATE map literals** (NOT aliased like PR-08's CC). Reference's two fields are identical maps; we duplicate to avoid mutate-leak risk. PR-09-D06 deferred suggests an explanatory comment in a future polish pass.
  - **`Cmd.Env` for AR has 2 entries** (`ARCADIA_ROOT_DISTBUILD`, `DYLD_LIBRARY_PATH`); CC's has 1 (`ARCADIA_ROOT_DISTBUILD` only). Different rules emit different env shapes — pin per-rule, not globally.

- **PR-04** (2026-05-07) — Comparator L0 (topology). `compare.go` defines `CompareReport{L0, L0Note, Skipped[]int}` + `Compare(want, got *Graph, maxLevel int) *CompareReport`. `compare_topology.go` implements the algorithm: per-node fingerprint = `sha1(kv.p || NUL || sorted(child fingerprints joined by NUL) || NUL)` truncated to 22-char base64url, computed in topological order (Kahn's BFS over UID→Node lookup, sorted-UID seeding for determinism, sorted children before enqueue). `L0 = matched / max(len(wantFPs), len(gotFPs))` via lockstep walk over two pre-sorted slices. Cycle in input → `ThrowFmt("cycle ...")`. `cmdCompare` rewired per PR-03 pattern (ContinueOnError + SetOutput(io.Discard) + errors.Is(flag.ErrHelp) + 2 positional args + `--level` int default 3 + `printCompareUsage` helper). Files: `compare.go`, `compare_topology.go`, `compare_topology_test.go`, modified `main.go`. Verification: `go build/vet/gofmt` clean; 6 new tests PASS (60 total); **real-graph self-match `L0: 100.00%` (3730 of 3730 fingerprints matched), byte-deterministic across runs** — load-bearing acceptance signal hit.
  Two review rounds. Round 1 (5 defects, 0 major): D01 6 blank-after-`{` regressions in compare_topology.go (PR-11 D01-D08 pattern recurrence); D02 dead `if maxLevel >= 0` wrapper after negative-level reject; D03 renumbered-UID test passed vacuously without sanity-asserting UIDs differed; D04 nil-graph diagnostic with inverted booleans (`want=false` for nil); D05 missing blank before `for byUID`. **All 5 fixed**. Round 2 (1 nit, deferred): D06 same blank-after-`{` pattern in compare_topology_test.go (test file, missed by D01 sweep) — defer to next compare-test edit.
  Surprises / constraints future PRs must respect:
  - **Fingerprint algorithm pinned**: `sha1(kv.p || NUL || sorted-children-joined-by-NUL || NUL)`, base64url-truncated 22-char. NUL separator prevents `kv.p="AB"+children=["C","DE"]` colliding with `kv.p="ABC"+children=["","DE"]`. PR-05/PR-06 must NOT change this — comparator-level invariance depends on it.
  - **`--level` default = 3**: anticipates full L0..L3 ladder. PR-05 adds L1+L2; PR-06 adds L3. Each unimplemented level shows in `Skipped []int` until wired.
  - **L0 algorithm uses `kv.p` ONLY** for kindness; other `kv` keys belong to L1/L2/L3 territory. PR-05 (L1) introduces them.
  - **Cycle detection in comparator** is defensive — real ymake graphs are DAGs. Throws on cycle so a malformed input fails loud, not silent.
  - **Empty-graph branch** returns vacuous L0=1.0 (denom=0 path); unreachable in production because `LoadReference` rejects empty graphs upstream.
  - **`cmdCompare` pattern** is now the second `cmdInspect`-shape subcommand; PR-10's `cmdGen` MUST adopt the same flag-handling pattern (D17 cross-cutting reaffirmed).
  - **D14 audit shape**: every `range` over a map in compare_topology.go either feeds a `sort.Strings`'d output OR is a presence-check / counter. No raw map iteration in the determinism-critical hash inputs.
  - **`Compare(nil, ...)` and `Compare(..., nil)`** throw with specific "want graph is nil" / "got graph is nil" messages (D04 fix). Catch at boundary.

- **PR-05** (2026-05-07) — Comparator L1 (kv.p, target_properties, outputs) + L2 (inputs, tags, requirements). Files: `compare_props.go` + `compare_props_test.go`, modified `compare.go` (CompareReport extended with L1/L2/L1Note/L2Note + WantOnly/GotOnly fields; Compare runs L1+L2 when maxLevel>=1 and >=2 respectively; one shared pairing build) and `main.go` (cmdCompare prints L1/L2 lines per --level). Pairing strategy: `(outputs[0], platform)` — NOT outputs[0] alone, because real graph has 11 outputs[0] collisions between target/host platforms (verified: 22 nodes, all `default-linux-aarch64`/`default-linux-x86_64` pairs). Platform is pairing-discriminator only, NOT in L1/L2 compared-fields. `pairByOutput` throws on duplicate-key collisions defensively. Per-pair match: ALL specified fields must match (boolean conjunction). Real-graph self-match: **L0/L1/L2 = 100.00% / 100.00% / 100.00%** (3730 of 3730), byte-deterministic.
  Two review rounds. Round 1 (6 defects, 0 major): D01 indexByUID docstring claimed throw on dupe but body silently overwrote (added throw); D02 --level flag usage stale (updated); D03 L1Note/L2Note phrasing misrepresented Y when graphs differ (note format reworked); D04 wantOnly/gotOnly returned but discarded (threaded into CompareReport); D05 false "alphabetically ordered inputs" comment (rewritten to honest "ymake-deterministic order" rationale; 7 of 3730 nodes have non-alphabetical inputs); D06 compareL1 docstring lied about note format (updated). **All 6 fixed**, D03/D04/D06 in one stroke. Round 2 (2 nits, deferred): D07 stale L1Note inline-comment example, D08 weakened test-substring assertion. Both deferred to next compare-edit cycle.
  Surprises / constraints future PRs must respect:
  - **Pairing key is `(outputs[0], platform)`** — pinned in `pairByOutput`. Future host/target merge work (M4) MUST preserve this; don't strip the platform component from the key.
  - **Real graph has 11 outputs[0] collisions** (target/host duplicates of same `.o`). Any future code that uses `outputs[0]` as a unique identifier is wrong. Use platform-qualified key.
  - **Note format pinned**: `"%d matched / %d pairs / %d unpaired-want / %d unpaired-got"` for L1/L2. PR-06's L3 should follow the same shape.
  - **`WantOnly`/`GotOnly` available in CompareReport** — sorted UIDs of nodes with no pair. Future `--verbose` mode in cmdCompare can dump these for debugging diffs.
  - **L1/L2 percentage denominator = `max(len(want.Graph), len(got.Graph))`** — unpaired nodes count against the percentage. The note format makes this explicit (matched < pairs ≤ max).
  - **`pairByOutput` throws on duplicate (outputs[0], platform) keys** — defensive guard against future reference graphs that violate the uniqueness assumption. If it ever fires in production, the comparator needs a richer pairing key (e.g. add `kv.p` or use full-outputs hash).
  - **Inputs are NOT alphabetical in g.json** for ~7 of 3730 nodes (mixed `$(BUILD_ROOT)` / `$(SOURCE_ROOT)` paths with `vcs_info.py` mid-list). PR-08+/PR-10 emitter MUST reproduce ymake's exact non-alphabetical order, not sort-and-emit. The yatool emitter's input ordering is a per-rule decision, not a global sort.

- **PR-06** (2026-05-07) — Comparator L3 (cmd_args + env byte-exact). Files: `compare_cmd.go` + `compare_cmd_test.go`, modified `compare.go` (CompareReport.L3/L3Note added; `highestImplementedLevel` 2→3; Compare runs L3 when maxLevel>=3) + `main.go` (cmdCompare prints L3 line; --level usage updated) + `compare_props_test.go` (TestCompareL1L2_LevelGating updated: assertion `Skipped == [3]` for `--level=3` rewritten to `Skipped == [4]` for `--level=4` — direct consequence of L3 implementation, executor-flagged judgment OK). L3 reuses PR-05's pairing (`pairByOutput` keyed on `(outputs[0], platform)`). Per-pair match: `len(want.Cmds) == len(got.Cmds)` AND for each i `stringSliceEqual(want.Cmds[i].CmdArgs, got.Cmds[i].CmdArgs)` AND `stringMapEqual(want.Cmds[i].Env, got.Cmds[i].Env)` AND `stringMapEqual(want.Env, got.Env)`. Real-graph self-match: **L0/L1/L2/L3 = 100.00% / 100.00% / 100.00% / 100.00%** (3730 of 3730 across all levels) — load-bearing acceptance hit.
  Two review rounds. Round 1 (3 minor): D01 per-cmd Env equality branch untested; D02 cloneNode shallow-copies Cmd internals (alias trap); D03 nil-vs-empty Env map false-negative risk for emitter-vs-reference comparison (forward-looking, unblocks PR-10). **All 3 fixed**: D02 deep-copies Cmds[i].CmdArgs and Cmds[i].Env in cloneNode; D01 added TestCompareL3_DifferentCmdEnvDropsBelow1; D03 introduced `stringMapEqual` helper that treats nil and empty as equal, replaced both `reflect.DeepEqual(...Env, ...Env)` calls in `l3Match`, dropped `reflect` import.
  Round 2 (1 minor + 1 nit): **D04 (REGRESSION introduced by D03)** — `stringMapEqual` used single-value map index `b[k] != v`, returning `""` for missing keys, so `{"x":""}` vs `{"y":""}` wrongly equated; **fixed** with two-value index `if v2, ok := b[k]; !ok || v2 != v` + new test assertion. D05 nit blank-line in cloneNode — fixed.
  Surprises / constraints future PRs must respect:
  - **`stringMapEqual` two-value index pattern** is now the project standard for nil-vs-empty-tolerant map equality. Any future map-equality helper MUST use two-value index, NOT `b[k] != v` — the latter conflates "key missing" with "key present, empty value".
  - **`stringMapEqual` semantics**: nil/{}, {}/nil, nil/nil all return true. Same-content maps return true regardless of key insertion order. Different-key maps with empty-string values correctly return false (D04 regression-test pinned).
  - **`l3Match` uses `stringMapEqual`, not `reflect.DeepEqual`** for both per-cmd Env and top-level Env. Future per-rule Env emitters MAY safely emit nil for empty Env without breaking byte-exact comparison against `"env": {}` decoded reference.
  - **`cloneNode` deep-copies Cmds internals** (CmdArgs slice + Env map). L3 tests rely on this — any future test that mutates `clonedNode.Cmds[i].Env[k] = ...` will only affect the clone, not the original.
  - **`highestImplementedLevel = 3`** in compare.go. Bumping to 4 in a future PR requires (a) implementing the new level, (b) updating cmdCompare's "skipped" line semantics, (c) updating `TestCompareL1L2_LevelGating`'s `--level=4 → Skipped==[4]` assertion to point at the next unimplemented level.
  - **Comparator is feature-complete for M1.** All four levels operational; real-graph self-match hits 100% across the board. PR-10's vertical slice will compare its 2-node emitted subgraph vs the reference and must hit L3=100% on those 2 nodes (and proportionally low percentages on L0/L1/L2 against the full 3730-node reference, since the slice is a tiny fraction of the whole graph).

- **PR-10** (2026-05-07) — **M1 vertical slice closer.** `gen.go` ships `Gen(cfg PlatformConfig, sourceRoot, targetDir string) *Graph`: parses `<sourceRoot>/<targetDir>/ya.make` via PR-07's `ParseFile`, walks AST to extract module type / SRCS / PEERDIR, validates (LIBRARY-only, no PEERDIR, non-empty SRCS — PEERDIR-present check fires before SRCS-empty for clearer diagnostics), emits CC per src via PR-08's `EmitCC` + AR via PR-09's `EmitAR` (collected NodeRefs threaded through), `emitter.Result(arRef)`, `Finalize(emitter)` returns `*Graph`. `cmdGen` rewired with the project-standard subcommand pattern: `flag.ContinueOnError` + `fs.SetOutput(io.Discard)` + `errors.Is(err, flag.ErrHelp)` discrimination + three flags (`--target`, `--out`, `--source-root` defaulting to `/home/pg/monorepo/yatool_orig`) + `printGenUsage(io.Writer)` helper. `writeGraph(out, *Graph)` helper uses `json.NewEncoder + SetEscapeHTML(false)` (D16) + `SetIndent("", "  ")` for readability + throw-style deferred Close. **PR-01-D05 ceremony retired** — `_ = flag.NewFlagSet("gen", flag.ExitOnError)` replaced with real flag handling; `"flag"` import is now load-bearing.
  Test: `TestGen_BuildCowOn_TwoNodeSubgraph_L3MatchesReference`. Skip-if-missing on `/yatool_orig/build/cow/on/ya.make`. Builds 2-node REFERENCE SUBGRAPH filtered by output-prefix AND `n.Platform == TargetCfg.Name` (executor's in-test adjustment — reference has 4 build/cow/on nodes, 2 platforms × {CC, AR}; filtering keeps the comparison 2-vs-2). Compare(subgraphRef, our, 3) asserts **L0/L1/L2/L3 all == 1.0 EXACTLY**.
  Plus 4 cmdGen flag-handling tests (TestCmdGen_HelpFlag, _UnknownFlag, _MissingTarget, _MissingOut) mirroring cmdInspect equivalents — established pattern coverage.
  Two review rounds. Round 1 (6 defects, 0 major): D01 double-space in printGenUsage; D02 no automated cmdGen flag tests; D03 CC output-path formula duplicated cc.go vs gen.go (deferred — refactor of EmitCC signature scoped to early M2); D04 writeGraph dropped Close error; D05 diagnostic order (SRCS-empty before PEERDIR-present); D06 hardcoded sourceRoot duplicates referenceGraphPath prefix. **5 fixed, D03 deferred.** Round 2: NO NEW DEFECTS.
  **Acceptance**: `./yatool gen --target build/cow/on --out our.json && ./yatool compare --level=3 our.json /yatool_orig/g.json` prints `2 matched / 2 pairs / 0 unpaired-want / 3728 unpaired-got` at all 4 levels — both emitted nodes (CC+AR for `lib.c`) match reference byte-exactly. M1 closed.
  Surprises / constraints future PRs must respect:
  - **Reference graph has 4 nodes per leaf module** (not 2) — 2 platforms × {CC, AR}. M2's recursive parsing MUST handle this when iterating `library/cpp/archive` etc., either by emitting both platforms (M4 host pass) or by filtering reference subgraph to the target platform (M2 stub host).
  - **`Gen` is M1-leaf-only.** PR-10 explicitly throws on PROGRAM, on PEERDIR-present, on empty SRCS. M2 generalizes: must extend rule emitter for those cases (and refactor EmitCC's signature per PR-10-D03 deferred to remove path-formula duplication).
  - **`writeGraph` uses pretty-print + SetEscapeHTML(false).** Pretty-print is fine because LoadReference decodes structure, not raw bytes. SetEscapeHTML is load-bearing for D16 (matches canonicalNodeBytes output).
  - **`--source-root` defaults to `/home/pg/monorepo/yatool_orig`.** Hardcoded in main.go. M2/M3 may want to make this configurable via env var or detect via .arcadia.root marker; for now hardcoded.
  - **`var sourceRoot = filepath.Dir(referenceGraphPath)`** in gen_test.go shares the path with gjson_test.go's `referenceGraphPath` const. Single source of truth for upstream-snapshot location across tests.
  - **`Result` field semantics on partial subgraphs.** PR-10's gen marks the AR node as result. Reasonable for a single LIBRARY. M2's multi-module slice (when recursive PEERDIR support lands) will need to decide: result = top-level binary's LD node? all leaves? Defer the decision until PR-13/M2 lands.
